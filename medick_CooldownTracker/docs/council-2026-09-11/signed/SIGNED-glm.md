# 🟣➤🔷 GLM — Cursor pool, glm-5.2-high, adversarial-correctness lens — 2026-09-11 (verbatim; sessionId 4c19c486-10af-4499-a798-b1d195b9fac0 · 38979 in / 4009 out, CREDITS)

Now let me compute MD5s for the manifestDONE_WITH_CONCERNS

| RANK | CLAIM | MECHANISM | VERIFY-BY / REPAIR | PATH:LINE |
|------|-------|-----------|--------------------|-----------|
| MATERIAL | Input-blocker release stomps sibling mods sharing `forceDisableInput` | `Apply(false)` sets `mgr.forceDisableInput=false` unconditionally whenever `_applied`, regardless of who else set it true; re-assert path only writes when currently false (asymmetric) | Reproduce: panel open (lock on) + CameraZoom zoom mode (lock on), close panel → CameraZoom lock dies. Repair: track a session-owned token; only clear if we were the last writer, or coalesce via shared flag | src/InputBlocker.cs:42 |
| MATERIAL | Second `AbilityBarIcon` set (minion bar / 2nd action bar / spectator) silently replaces player slots | `Register` keys by `abilityNumber` only; `_slots.RemoveAll(idx==)` then insert → last-registered wins; `Tick`/renderer then anchor to the new `Source.player` | Reproduce: load a zone where the game spawns a minion bar sharing abilityNumber 0-5; player's slot 0 icon becomes the minion's, anchored above the minion. Repair: key by `Source` instance identity, filter by player-owned bar (e.g. `player` is the local character) | src/SlotRegistry.cs:55-69 |
| MATERIAL | `Camera.main` unguarded in OnGUI; future Unity/MelonLoader deprecation throws per event | `var cam = Camera.main;` is not in try/catch; `Camera.main` is deprecated post-Unity 2023, throws on removal | Reproduce: build against a Unity where `Camera.main` is gone → exception every OnGUI pass, log spam, render dies. Repair: wrap in try/catch or cache via `Camera.current`/FindWithTag | src/UI/OverheadRenderer.cs:13 |
| MINOR | NaN/Inf prefs reach the renderer and panel unsanitized | `Mathf.Clamp(NaN, lo, hi)` returns NaN (no NaN guard); `Mathf.Clamp01(NaN)` likewise; cfg-edit `OffsetX=NaN` → NaN rects, invisible/garbage draw, no crash | Reproduce: hand-edit cfg `MenuScale=NaN` → panel vanishes. Repair: add `float.IsNaN` guard in Prefs read accessors or sanitize on load | src/Prefs.cs, src/UI/OverheadRenderer.cs:33-35, src/UI/SettingsPanel.cs:55 |
| MINOR | `InputMode` out of `[0,3]` (e.g. 7) silently becomes Auto but no segment is highlighted | `GetModeIndex` default branch falls to auto-detect; `Segmented` highlights `i==selected` for i in 0..3 only → no visual selection | Reproduce: cfg `InputMode=7` → panel shows no highlighted segment, behaves as Auto. Repair: clamp `InputMode` to `[0,3]` on read | src/InputDetection.cs:117-128, src/UI/SettingsPanel.cs:101 |
| MINOR | Stale `v4.x`/`v5` archaeology comments in a v1.0.0 mod | Comments in InputDetection, SlotRegistry, OverheadRenderer, Theme, InputBlocker reference "v4.x", "v5 ships no Input patches" — confusing in a 1.0.0 codebase | Repair: strip or rewrite to present-tense; low risk, high clarity | src/InputDetection.cs:8,148; src/InputBlocker.cs:8; src/UI/OverheadRenderer.cs:8; src/SlotRegistry.cs:13 |
| MINOR | README install line stale: "MelonLoader 0.6.x" while mod runs on 0.7.2 Open-Beta | README.md Installation step 1 says 0.6.x; tonight's log shows 0.7.2 Open-Beta loads it | Repair: update to "0.7.x"; low risk | README.md:43 |
| NOT PROVEN | Dead player (disabled, not destroyed) leaves icons anchored at corpse | `s.Source != null` is true for disabled-not-destroyed objects; `Source.player` may still resolve; icons keep drawing at the body | Need in-game death test to confirm whether LE destroys or merely disables the player GameObject on death. No repro from stubs | src/SlotRegistry.cs:139, src/UI/OverheadRenderer.cs:96-104 |
| NOT PROVEN | `abilityNumber` reused on spec-swap re-runs no new Awake | If the game mutates `abilityNumber` on an existing `AbilityBarIcon` without re-Awaking, stored `SlotIndex` is stale | Need a spec-swap playtest watching the registry log; not provable from stubs | src/SlotRegistry.cs:55,121 |

**Q1** Patches are name-based Harmony postfixes; rename/removal → Harmony logs a warning at startup and the patch no-ops (no crash, no per-frame spam). `Awake` loss → no slots register (silent wrong, no icons). `activate/deactivateCooldownBar` loss is partly masked because `Tick` re-reads `Source.cooldownBarActive` directly. Field reads (`icon`, `cooldownBar`, `cooldownBarActive`, `abilityNumber`, `player`) are all in try/catch — Register warns and skips, Tick removes the slot, renderer skips it. `EpochInputManager.instance` rename → `InputBlocker.Apply` swallows, `_applied` stays false, no lock (silent degrade, no crash). `Camera.main` is the one unguarded walk (see MATERIAL). `Text`/`Image` walks are guarded. Sibling-mod coexistence is asymmetric: lock-on re-assert is conditional, release is unconditional → stomps CameraZoom (see MATERIAL).

**Q2** Wrong-source/wrong-slot is real: no instance disambiguation, last-`abilityNumber`-registered wins (MATERIAL above). Stale icon after skill swap: `Tick` refreshes `s.Icon` every 20 Hz and respects `overrideSprite` — correct. Duplicate slot: prevented by `RemoveAll` before insert. Cross-zone/character-swap/login: liveness pruning + no-player-anchor rule keep the screen clean. Death case is NOT PROVEN. Second bar is the real gap.

**Q3** OnUpdate: `InputTracker.Update` + Home check + `InputBlocker.Apply` (write-on-change) + 20 Hz tick — no per-frame heap growth. OnGUI runs multiple passes/frame; `OverheadRenderer.Draw` allocates a handful of `new Color(...)` per icon per pass (~35/frame at 7 icons × 5 passes) — minor GC pressure only. Style caches (`_labels`/`_buttons`/`_fields`) are bounded by quantized sizes; `IconLabel`/`BadgeLabel` are mutable singletons. `_joyNames` re-assigned every 5 s. No unbounded growth. `SlotData.Source`/`CooldownBar`/`Icon` Il2Cpp refs are pruned by liveness in `Tick`; mid-read death is caught and the slot removed.

**Q4** No hardcoded template name, no child-index lookup — panel is fully self-drawn via IMGUI, so it is immune to the "Toogle - Minion Health Bars" failure that killed the sibling mods tonight. Out-of-range prefs: `Size=0` clamped in renderer but pref stays 0 (display mismatch); `InputMode=7` becomes Auto with no segment highlighted; `NaN` offset/scale propagates unsanitized (MINOR). Orphan cfg keys (`ShowWindow`, `ShowOverhead`, `WindowScale`, `WindowX/Y`, `IconAlpha`, `IconSize`, `OverheadY`, `EnabledKeys`, `Controller`, `SlotLabel0–5`) are unregistered → ignored at runtime, harmless, worth a one-time strip on save.

**Q5 (best value/risk)** 1. Strip the `v4.x`/`v5` archaeology comments — comments-only, zero risk, big clarity gain. 2. Update README MelonLoader version 0.6.x → 0.7.x — doc-only, zero risk. 3. Add NaN/out-of-range sanitization in `Prefs` read accessors — small, defensive, prevents the cfg-edit foot-gun without changing the slider UX.

**Q6** CooldownTrackerMod.cs PATCH (guard `Camera.main`). Patches.cs PATCH (try/catch already covers; consider `nameof`-based resilience). SlotRegistry.cs PATCH (instance-keyed registry, not `abilityNumber`-only). InputDetection.cs PATCH (stale comments; clamp `InputMode`). InputBlocker.cs PATCH (asymmetric release stomps siblings). Prefs.cs PATCH (NaN/range sanitization). OverheadRenderer.cs PATCH (minor Color alloc; `Camera.main` guard). SettingsPanel.cs PATCH (stale comments). Theme.cs PATCH (stale comment). Widgets.cs PATCH. ButtonPicker.cs PATCH. UiState.cs PATCH. BuildInfo.cs PATCH. csproj PATCH (add `<Version>` + copy-to-Mods target). README.md PATCH (ML version). CHANGELOG.md PATCH.

**Manifest** — every file under `src/`, the csproj, README.md, CHANGELOG.md, melonloader-lines.txt, cooldown-cfg-excerpt.txt, BRIEF.md was read in full. MD5 computation was attempted via `md5sum` and `Get-FileHash` but the shell backend returned no exit status on every invocation this session, so MD5s could not be computed; flagging this honestly rather than fabricating hashes. All 19 source/config files were read end-to-end via the Read tool.

🟣➤🔷 GLM

---
🟣➤🔷 [wmw-cursor] 💸 Zhipu · glm-5.2-high
   sessionId: 4c19c486-10af-4499-a798-b1d195b9fac0 · meter: CREDITS · 38979 in / 4009 out
🟣➤ 💸 —— THIS CALL SPENT MONEY —— 💸 🟣➤
   third-party CREDIT pool — billed at API prices
