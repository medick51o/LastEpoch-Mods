# 🔵 Codex (Astra) — gpt-6-astra, architecture lens — 2026-09-11 (verbatim; threadId 01a08f82-5998-7cb3-8473-394a841eb60b)

DONE_WITH_CONCERNS
| Rank | Claim | Failure mechanism | Verify-by / repair direction, path:line |
|---|---|---|---|
| MATERIAL | Q1: Input blocking has no ownership protocol. | Opening while another owner already holds `forceDisableInput=true` still sets `_applied`; closing unconditionally writes false. Reassertion competes with Terrible Zoom by update order. `_applied` also survives manager replacement. | Hold Zoom's lock, open/close this panel, and trace the flag in both update orders; repeat across manager replacement. Coordinate an owner-based lock service across participating mods; saving the previous boolean alone cannot solve concurrent ownership. `src/InputBlocker.cs:21`, `src/InputBlocker.cs:35`, `src/InputBlocker.cs:40`. |
| MATERIAL | Q2: Registry ownership is slot-number-only. | Any second `AbilityBarIcon` set replaces the first set's matching indices. Events from the displaced source then mutate the replacement's state. If the replacement dies, pruning does not recover the original live source. | Register sources A/B with the same index; deactivate A, then destroy B. Filter to the local player's authoritative bar, validate indices, and match event source identity. `src/SlotRegistry.cs:52`, `src/SlotRegistry.cs:65`, `src/SlotRegistry.cs:120`, `src/SlotRegistry.cs:141`. |
| MATERIAL | Q2: Live replacements can retain stale artwork and cooldown fill. | Tick adopts a sprite only when non-null and reacquires `cooldownBar` only when the cached bar becomes Unity-null. A surviving old bar supplies fill while the current source supplies its active flag. `SlotIndex` never refreshes after registration. | Replace the bar without destroying the old Image; clear/change the icon; change `abilityNumber` after Awake. Refresh the complete source snapshot, including nulls, and revalidate identity/index. Ordinary non-null sprite swaps already refresh. `src/SlotRegistry.cs:147`, `src/SlotRegistry.cs:149`, `src/SlotRegistry.cs:153`, `src/SlotRegistry.cs:55`. |
| MATERIAL | Q2: "Living player on screen" is not enforced. | The anchor checks only object existence and positive camera-space depth—not death, active hierarchy, local ownership, screen bounds, or gameplay state. A surviving hidden bar/player can remain drawable. Settings and Move/picker state persist across transitions until explicitly closed. | With a cooldown active, die, disable the bar, switch characters, and return to login while objects survive; repeat with settings open. Add explicit gameplay/owner eligibility and transition resets. Actual game retention behavior is NOT PROVEN. `src/UI/OverheadRenderer.cs:96`, `src/UI/OverheadRenderer.cs:101`, `src/UI/UiState.cs:6`, `src/UI/SettingsPanel.cs:222`. |
| NOT PROVEN | Q1: Future Harmony target changes are not isolated cleanly. | Awake, activateCooldownBar, and deactivateCooldownBar are unconditional string-targeted patches without a capability gate. Missing/ambiguous methods can fail Harmony installation; exact MelonLoader continuation behavior was not exercised. Losing Awake registration leaves no discovery fallback; losing either cooldown hook can be compensated by Tick if installation continues. | Test renamed/overloaded targets against regenerated interop assemblies; resolve each target explicitly, isolate optional hooks, and emit one actionable compatibility diagnostic. `src/CooldownTrackerMod.Patches.cs:16`, `src/CooldownTrackerMod.Patches.cs:24`, `src/CooldownTrackerMod.Patches.cs:32`, `src/SlotRegistry.cs:151`. |
| NOT PROVEN | Q1: Field-access catches do not establish safety under binary incompatibility. | For ordinary managed read failures: registration warns; Tick removes the slot silently; cooldown postfix reads fail silently; player-anchor reads hide icons; input-manager failures silently lose blocking. Missing generated accessors/type initialization can fail before an inner catch runs; unsafe stale native metadata has no demonstrated crash protection. | Exercise removed/type-changed `icon`, `cooldownBar`, `cooldownBarActive`, `abilityNumber`, `player`, and input-manager accessors. Distinguish regenerated-stub incompatibility from stale-stub/native mismatch; disable incompatible features once. Log spam or process crash under those scenarios is NOT PROVEN. `src/SlotRegistry.cs:72`, `src/SlotRegistry.cs:126`, `src/SlotRegistry.cs:169`, `src/UI/OverheadRenderer.cs:105`, `src/InputBlocker.cs:44`. |
| MINOR | Q1/Q2: Camera and hierarchy discovery have silent semantic failure modes. | No tagged `Camera.main` cleanly suppresses overhead output; the wrong tagged camera misanchors it. Camera lookup itself is outside a catch. Text discovery takes the first short plausible child string, rejects numeric bindings, and caches success permanently; TMP conversion yields fallback labels. Child Image traversal is debug-only. | Retag cameras, reorder child Text components, bind a numeric key, and rebind after discovery. Use an authoritative camera/binding source or refresh on binding changes. Image hierarchy changes alone affect debug output; production artwork uses `icon.icon`. `src/UI/OverheadRenderer.cs:24`, `src/InputDetection.cs:156`, `src/InputDetection.cs:164`, `src/SlotRegistry.cs:159`, `src/SlotRegistry.cs:92`. |
| MATERIAL | Q4: Preferences lack finite-value validation. | `Size=0` safely renders at 32; finite MenuScale/Alpha values are clamped. Offsets are used directly, and NaN survives ordinary clamps into GUI geometry; NaN panel position/scale can make recovery controls unusable. InputMode=7 falls back to automatic resolution but shows no selected mode or Auto controls. | In a disposable test configuration, supply NaN offsets/panel scale and out-of-range modes; verify parser acceptance separately. Normalize loaded values using finite checks, ranges, and defaults. No current-config corruption was found. `src/Prefs.cs:39`, `src/UI/OverheadRenderer.cs:34`, `src/UI/OverheadRenderer.cs:40`, `src/UI/SettingsPanel.cs:54`, `src/InputDetection.cs:115`. |
| MINOR | Q4: Valid scale settings can place controls below the screen. | The entire panel has fixed computed height; only the skill list scrolls. Seven slots in forced mode need approximately `757 × scale` pixels, so scale 2 exceeds a 1080px screen; clamping its origin cannot make the bottom reachable. | Open seven slots at scale 2 on 1080p. Cap scale to available height or scroll the full content. `src/UI/SettingsPanel.cs:73`, `src/UI/SettingsPanel.cs:78`, `src/UI/SettingsPanel.cs:88`, `src/UI/SettingsPanel.cs:253`. |
| MINOR | Q3: Static references and owned Unity resources lack teardown. | Closed-panel `_rows` and camera-less renderer `_buf` retain old SlotData references until another snapshot. Theme creates textures/font without destruction; `_ready` is set before initialization finishes, so an initialization exception leaves partial state latched. Unity-null pruning does not clear dormant buffers. | Populate/close the panel, remove the camera, destroy sources, then inspect retained references; exercise deinitialization and failed Theme initialization. Clear buffers/state and destroy only mod-owned resources; mark Theme ready after success. `src/UI/SettingsPanel.cs:20`, `src/UI/OverheadRenderer.cs:17`, `src/UI/Theme.cs:52`, `src/UI/Theme.cs:122`, `src/CooldownTrackerMod.cs:71`. |
| MINOR | Q3/Q5: Allocation-free rendering claims are too broad. | Every open-panel GUI event creates GUIContent and formatted strings; an open picker creates five glyph arrays per Draw. Layout/input/repaint passes repeat this work. Cached styles avoid repeated style construction, but do not remove these allocations or interop wrapper costs. | Profile Layout/Repaint separately with panel/picker open; cache fixed glyph arrays and measurement content. `src/UI/Widgets.cs:17`, `src/UI/Widgets.cs:102`, `src/UI/SettingsPanel.cs:199`, `src/UI/SettingsPanel.cs:298`, `src/UI/ButtonPicker.cs:92`, `CHANGELOG.md:66`. |
| NOT PROVEN | Q1/Q2: Future atlas packing can silently show incorrect artwork. | Tight-packed sprites fall back from `textureRect` to original `rect`; rotated packing is not handled. Those coordinates need not describe the atlas region correctly. Current skill-atlas exposure was not checked. | Test tight-packed and rotated skill sprites; use sprite UV/mesh-aware drawing or explicitly reject unsupported packing. `src/UI/Theme.cs:248`. |
| MINOR | Q5: Documentation overstates lifecycle and coexistence guarantees. | Write-on-change/no-stomping comments contradict reassert/release behavior; "v5" comments remain in v1.0.0; installation names 0.6.x while supplied evidence uses 0.7.2. "Ready flash" is only a near-ready border before active filtering removes the icon. | Correct claims against current behavior and explicitly state tested versions. `src/InputBlocker.cs:9`, `src/InputBlocker.cs:11`, `src/InputDetection.cs:13`, `README.md:10`, `README.md:42`, `README.md:65`, `CHANGELOG.md:57`, `src/UI/OverheadRenderer.cs:140`, `src/SlotRegistry.cs:204`. |

Manifest — computed MD5s; paths relative to the review directory except the assembly:
```text
BRIEF.md                           A18EF131B24D933DB98B67F58AC54D0B
src/BuildInfo.cs                    63D22A095E2E81F85B89F9F461EE0481
src/CooldownTrackerMod.cs           CD31698562476332E754DE9216021939
src/CooldownTrackerMod.Patches.cs   BBA64CFE93F8715C837F1DBFA2BEC50D
src/InputBlocker.cs                 0BE152AC740D1A3202B6E77024D8B881
src/InputDetection.cs               C5B2F6492DD341375B63D32C36D76843
src/Prefs.cs                        01DF7052D7BFF469691C06480E9FD071
src/SlotRegistry.cs                 047B4A5BBA100BC7C14FD2D972D4A4A9
src/UI/ButtonPicker.cs              F0573919665C5D3068B0AEB5DFE7113C
src/UI/OverheadRenderer.cs          CED8939C78C72E81B76995E5D10E1D48
src/UI/SettingsPanel.cs             07C157ECD6B01B3AB804290EFA6175A1
src/UI/Theme.cs                     718AE28117513F8909674EB5A69D50FB
src/UI/UiState.cs                   4C7830B7EE4933CEC1E747364994D057
src/UI/Widgets.cs                   572FAE9691C10857F3C67994CEFF3D60
medick_CooldownTracker.csproj        A1B3AA00B826116F7701E527D968A6C6
README.md                          54B600649E54F7338101ED7A8B8D180F
CHANGELOG.md                       D1444708A911C71164597934D8EE6F04
melonloader-lines.txt              D774DF21F4E34A40883E5994B595A74E
cooldown-cfg-excerpt.txt            B9F439C286755C45680CBBCAAA6EB500
C:/Program Files (x86)/Steam/steamapps/common/Last Epoch/MelonLoader/Il2CppAssemblies/Il2CppLE.dll  5FEF07528053F3A463EDA5255EC9A985
```

Q1 signature evidence: read-only ILSpy inspection confirms public instance `void Awake()`, `void activateCooldownBar()`, `void deactivateCooldownBar()`; `icon`/`cooldownBar`: Image; `cooldownBarActive`: bool; `abilityNumber`: int; `player`: GameObject. EpochInputManager exposes static `instance` and instance read/write bool `forceDisableInput`. These are generated interop properties over native fields; signatures are verified, native semantics and successful hook execution are not.

Q2 boundary: simultaneous duplicate numeric indices are prevented by RemoveAll, but competing owners overwrite each other. Destroyed sources prune on the next scheduled Tick, not immediately; surviving disabled objects do not prune. No scene/character/death callback resets the registry or panel (`src/SlotRegistry.cs:65`, `src/SlotRegistry.cs:139`, `src/CooldownTrackerMod.cs:25`).

Q3 execution/bounds: OnUpdate polls device activity/Home, applies the input guard, and schedules Tick using scaled deltaTime; joystick names refresh every five scaled seconds. Tick scans all slots, reads sprites/fill, retries missing bindings, and refreshes labels. Every OnGUI event runs Theme.Ensure, picker pre-input, camera/snapshot/anchor work and drawing, plus settings/picker work when open (`src/CooldownTrackerMod.cs:25`, `src/CooldownTrackerMod.cs:49`). Normal finite settings produce bounded integer-size style caches; picker chips clear each Draw. Registry size follows distinct live indices, with no enforced seven-slot bound; snapshot capacities retain their high-water marks. No ordinary per-frame unbounded growth is established. "Thread-safe" covers list access, not Unity main-thread affinity or mutable SlotData escaping through snapshots (`src/SlotRegistry.cs:191`).

Q4 configuration: the panel is self-drawn IMGUI, with no hardcoded native template name or child-index dependency (`src/UI/SettingsPanel.cs:90`). The listed orphan keys are unregistered and inert for this source—including ShowOverhead=false, which does not disable rendering. They are worth documenting/pruning during a deliberate migration, not silently deleting historical settings (`src/Prefs.cs:41`, `cooldown-cfg-excerpt.txt:2`).

Q5 highest-value low-risk cleanups: (1) reconcile version, lock, lifecycle and allocation claims; (2) hoist picker glyph arrays and fixed GUIContent out of repeated GUI passes; (3) document active preference keys and obsolete-key mappings before optional migration. No whole source module is demonstrated dead.

Q6 verdicts — PATCH means retain the design and make targeted repairs; REBUILD below means retain unchanged in the eventual validated build, not that compilation was performed.
| File | Verdict and basis |
|---|---|
| `src/CooldownTrackerMod.cs` | PATCH — add lifecycle resets/resource teardown and compatibility-failure containment. |
| `src/CooldownTrackerMod.Patches.cs` | PATCH — validate targets and isolate optional hooks without assuming all-or-nothing discovery succeeds. |
| `src/SlotRegistry.cs` | PATCH — enforce owner/source identity, valid indices, fresh references, and transition cleanup. |
| `src/InputBlocker.cs` | PATCH — replace unilateral shared-flag release with coordinated ownership. |
| `src/InputDetection.cs` | PATCH — invalidate binding caches and strengthen discovery; remove stale version comments. |
| `src/Prefs.cs` | PATCH — normalize finite numeric values and enum ranges while preserving active keys. |
| `src/UI/OverheadRenderer.cs` | PATCH — enforce gameplay eligibility and clear dormant references/drag state. |
| `src/UI/SettingsPanel.cs` | PATCH — support screen-height constraints, lifecycle resets, and cached measurement content. |
| `src/UI/Theme.cs` | PATCH — make initialization transactional and explicitly own/destroy generated resources. |
| `src/UI/Widgets.cs` | PATCH — cache fixed measurement content and consume validated slider values. |
| `src/UI/ButtonPicker.cs` | PATCH — cache glyph tables and reset modal geometry/state on lifecycle changes. |
| `src/UI/UiState.cs` | PATCH — centralize transient-state reset for close, transition and deinitialization. |
| `src/BuildInfo.cs` | REBUILD — its identity/version constants are coherent and need no architectural rewrite. |
| `medick_CooldownTracker.csproj` | REBUILD — retain explicit interop references and manually validate against supported assemblies; absent deployment targets are not a defect. |
| `README.md` | PATCH — correct tested loader version and unsupported behavioral guarantees. |
| `CHANGELOG.md` | PATCH — reconcile contradictory lock descriptions and qualify allocation/lifecycle claims. |
| `BRIEF.md`, `melonloader-lines.txt`, `cooldown-cfg-excerpt.txt` | REBUILD — retain unchanged as review evidence; none is production code to retire. |

Evidence gate: all required files read; MD5s computed; requested stub signatures inspected. No files modified, no build or game reproduction performed. Supplied load/save logs establish loading, not the edge-case behaviors above. Targeted patches are warranted; wholesale retirement is not supported.
🔵 Codex (Astra)
