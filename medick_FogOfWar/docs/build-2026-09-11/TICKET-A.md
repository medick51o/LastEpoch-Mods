# TICKET-A — Terrible fog_OF_war Tier A → v1.0.1 (extends the unreleased 1.0.1; NO version bump)

## TASK (the boss's words, verbatim)
"go thru all my release mods we havent went over tonight that have been release on nexusmods. have the council go through each one and do the extensive review i want astra doing the same work as we did on terrible tool tips and terrible inventory. just the released ones on nexusmods not the private or github one exclusives. go in automode for the night and prepare all the changes for me to review in the morning. if the astra seat runs out wait the 5 hours and pick up where you last left off."

## CONTEXT
Repo `C:\Users\andre\Downloads\LastEpoch-Mods\medick_FogOfWar` — source `medick_FogOfWar\src\*.cs`, build file `medick_FogOfWar\medick_FogOfWar.csproj` (NOTE the nested project folder; `docs\` is a SIBLING of the project folder, not inside it). MelonLoader + HarmonyX mod for Last Epoch 1.4.7, IL2CPP interop (`Il2Cpp`, `Il2CppLE.UI.Minimap` namespaces), net6.0, Unity 6000.0.42f1. The tree is ALREADY v1.0.1 (BuildInfo.cs + csproj `<Version>1.0.1</Version>`), unreleased and uncommitted — last night's BLIND leaf-only fix (see `docs\build-2026-09-10\`). Keep 1.0.1. Do NOT bump.
Council findings this ticket implements: `docs\council-2026-09-11\SYNTHESIS.md` (read for mechanism, not instructions; finding numbers below refer to its agreement table). Baselines + md5: `docs\build-2026-09-11\baseline\` (copies carry a `.baseline` suffix; `MD5SUMS.txt` lists the originals). The pre-ticket build of this exact source is green: 0 warnings, 0 errors, 24,064 B.
The csproj ALREADY has the `CopyToMods` target with `Condition="'$(DeployToMods)' != 'false'"` (added 2026-09-10) — verify it, do NOT add a second condition or a second target.

DO NOT change what any vision level does on its success path: the leaf-only hide target, the CanvasGroup-first / SetActive-fallback order, the `activeSelf` adoption guard, the restore-never-gated-on-minimap rule, the radius table, the `_writtenRadii` cap, the frozen identifiers (assembly name, prefs category/entry, cfg path). The CanvasGroup restore path must NOT gain a `SetActive(true)` (council finding 9 is a Tier B decision).

## ITEMS (all required)
A1 — `FogController.Hide` (src\FogController.cs ~190-228), make the CanvasGroup path transactional: (a) build the `HiddenTarget` snapshot (Alpha/BlocksRaycasts/Interactable read from the group) and `_hidden.Add(hidden)` BEFORE the three writes `alpha = 0f / blocksRaycasts = false / interactable = false`; (b) in the `catch` of that inner try: if the entry was added, try to write the snapshot values back to the group (roll back), then `_hidden.Remove(hidden)`; if the rollback itself throws, KEEP the entry in `_hidden` (ShowHidden will retry later), log one `MelonLogger.Warning` ("minimap hide partially applied — will restore on leaving BLIND") and `return` WITHOUT running the SetActive fallback (never two mechanisms on one target); (c) keep the existing destroy-of-added-group and the SetActive fallback for the clean-rollback case exactly as they are. Minimal diff; no new types. (Finding 1: Kimi, Astra)
A2 — Latch two warnings once per session with static bools: the `HasProtectedItemUI` refusal warning in `Hide` (~line 166) and the "apply failed" warning in `Apply` (~line 128). First occurrence logs as today (append " — further occurrences this session are silent"); later occurrences are silent. Behaviour otherwise unchanged (the refusal still returns; Apply still catches). (Finding 5: Astra, GLM)
A3 — `FogController.Hide`: before registering a NEW target (i.e. after the "already tracked" loop, before the `activeSelf` guard), prune `_hidden` of dead entries: remove every entry whose `GameObject == null` (Unity fake-null; wrap the test in try/catch and treat an exception as dead). One `_hidden.RemoveAll(...)` is fine. Do not touch `ShowHidden`. (Finding 3: Gemini, GLM, Astra, Kimi)
A4 — `FogController.OnMinimapAwake` (~74-90): the bare `catch { }` around the zone-default capture becomes a latched once-per-session `MelonLogger.Warning` ("zone default RevealRadius unreadable (<ex.Message>) — LIMITED/SCOUT scale off the 150 fallback until a game update restores it"). Still swallow after the first. (Finding 6: Kimi, Astra)
A5 — `Prefs.Save` (src\Prefs.cs ~26-31): the bare `catch { }` becomes a latched once-per-session `MelonLogger.Warning` ("prefs save failed (<ex.Message>) — your vision level may not persist to the next launch"). Keep `printmsg: false` and the Dbg line. (Finding 7: Kimi, Astra, GLM)
A6 — `NativeSettings.TemplatesAvailable` (src\NativeSettings.cs ~53-67), diagnostic only: when the toggle template or the header anchor is missing, the ONE latched warning must also list the direct child names of the settings root (`root.GetChild(i).name`, joined with " | ", capped at 60 names then "…"), built inside its own try/catch (on failure append "root children unreadable"). Format: existing detail + "; root children: " + list. This is so the real 1.4.7 row names can be read from the next MelonLoader log. Do not add new template candidates, do not change injection. (Finding 8: all four)
A7 — Comment/doc only, no code: (a) src\FogController.cs `OnMinimapUpdate` comment (~52-61) and src\FogOfWarMod.Patches.cs `Patch_MinimapUpdate` comment (~47-52): state that under the CanvasGroup path the hidden minimap STILL receives Update every frame (the object stays active) and the postfix then costs one enum compare plus the `IsHidden` scan; only the SetActive fallback silences Update. (b) SPEC.md rule 4 (~line 35-41): rewrite to the leaf-only law — BLIND hides ONLY `minimap.gameObject`, never an ancestor (the ground-label canvas can share an ancestor and labels spawn at runtime — 2026-09-10 incident); the hide uses a CanvasGroup (alpha 0, no raycasts, not interactable) when the object accepts one, `SetActive(false)` otherwise; if the leaf itself contains a `GroundItemLabel` or `UITooltipItem` the hide is refused and BLIND degrades to HARD behaviour with a warning; keep the "remember every object, restore never gated on a live Minimap" sentences. Also update the SPEC title line "v1.0.0" → "v1.0.1". (c) README.md: Installation step 1 → "Install MelonLoader 0.7.x (tested on 0.7.2 Open-Beta, game 1.4.7)"; the "Where it lives" section gets one honest paragraph: "On Last Epoch 1.4.7 the game renamed the settings row the mod clones from, so the Terrible fog_OF_war section currently does not appear (the log says `settings hierarchy changed`). Set `VisionLevel` in `UserData/medick_The_fogOFwar.cfg` (BLIND, HARD, LIMITED, NORMAL, SCOUT, ORACLE); it applies at the next zone. A fix is queued once the new row name is known."; file map: add `FogOfWarMod.Patches.cs` mention of the Minimap.Update BLIND re-assert patch (it lists only Minimap.Awake + settings). (Findings 4, 13, 14: all four)
A8 — Mechanical: (a) csproj: add an ItemGroup with `<Compile Remove="docs\**\*.cs" />` plus an XML comment saying it is an inert guard here (docs\ is a sibling of the project folder) kept for parity with the sibling mods; confirm the existing `Condition="'$(DeployToMods)' != 'false'"` on `CopyToMods` and do not duplicate it; `<Version>` stays 1.0.1. (b) CHANGELOG.md: EXTEND the existing "## v1.0.1 — BLIND no longer hides ground item labels" section (keep its two sentences) with a sub-list "Council pass (2026-09-11)" of one honest line per item A1–A7 and a line stating that on 1.4.7 the in-game settings rows do not build and the cfg is the control until the adapter is repaired. (Finding 16 + housekeeping)

## EXPECTED OUTCOME
1. From `medick_FogOfWar\medick_FogOfWar\` (the project folder): `dotnet build -c Release --nologo -v q -p:DeployToMods=false` → Build succeeded, 0 warnings, 0 errors.
2. NOTHING copied into `C:\Program Files (x86)\Steam\steamapps\common\Last Epoch\Mods\`: `medick_The_fogOFwar.dll` there must remain 24,064 bytes dated 2026-09-10 21:35 (md5 9f1681027eb49f74006400015551de9e). Check it and print the result.
3. Each item present, each with a one-line comment naming the council finding it closes (e.g. `// council 2026-09-11 #1`).
4. Report: status word first (DONE / DONE_WITH_CONCERNS / BLOCKED); build command + tail; per-item one-liner with file:line; the Mods-folder check; anything not done and why.

## CONSTRAINTS
Minimum change per item; no refactors; no new files; keep comment style; do not touch files outside the WRITE SET; no game run; no commit; no deploy; `--no-restore` is acceptable if NuGet config is unreadable in the sandbox.

## MUST NOT
No spawns, no sub-agents, no edits outside the WRITE SET, no commits, no copying anything into the game's Mods folder, no running the game, no change to success-path behaviour (see CONTEXT), no version bump, no `SetActive(true)` added to the CanvasGroup restore path, no deletion of `CreateEnumDropdown`.

## OUTPUT FORMAT
Status word first, then the four report parts above. Under 60 lines.

## WRITE SET
medick_FogOfWar\medick_FogOfWar.csproj · medick_FogOfWar\src\FogController.cs · medick_FogOfWar\src\FogOfWarMod.Patches.cs (comments only) · medick_FogOfWar\src\Prefs.cs · medick_FogOfWar\src\NativeSettings.cs · README.md · SPEC.md · CHANGELOG.md

## LAWS
"'I could not tell what you meant' is a good outcome. Propose, don't guess."
