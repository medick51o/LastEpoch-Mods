# Terrible fog_OF_war v1.0.1-dev — council synthesis (2026-09-11, overnight auto mode)
Seats (blind, read-only, packet C:\Sync\Projects\fog-review-2026-09-11, mirror commit a36542f = repo tree incl. the uncommitted BLIND fix + docs\build-2026-09-10 as context): 🔵 Astra (gpt-6-astra, architecture) · 🟢 Gemini (Antigravity, robustness / player experience; brain UNREPORTED) · 🟣➤🌙 Kimi k3-high (code quality, 💸) · 🟣➤🔷 GLM 5.2-high (adversarial player harm, 💸). ⚫ Grok benched. All four returned DONE_WITH_CONCERNS. Mirror `git status --porcelain` empty after all four seats. Cursor allowance: these were the last two calls of the 10/week window (10/10 used).
Provenance: NO gap. The DLL in the game's Mods folder (24,064 B, 2026-09-10 21:35, md5 9f1681027eb49f74006400015551de9e) is byte-identical to the repo's `bin\Release\net6.0` build (21:32) of this exact source. A fresh build of the same source in the mirror is green (0 warnings, 0 errors, 24,064 B). Astra and Gemini computed MD5s that match the mirror for every file; Kimi and GLM could not run a shell in the Cursor read-only sandbox and listed files without hashes (both said so rather than fabricate). Astra did not open the game stub; the conductor's stub grep (BRIEF) stands.
Settings-UI fact, settled by the code: on 1.4.7 the toggle template `Toogle - Minion Health Bars` is not found, `TemplatesAvailable` returns false, and NO rows are built (all four seats). The level values that differed between launches tonight were read from the cfg at startup; nothing in this source applies a cfg edit live — it lands at the next `Minimap.Awake` (zone-in). Astra: any claim of an in-game level change tonight is NOT PROVEN by this packet. Conductor: the in-game rows were unavailable in every session tonight; the boss's level changes must have been cfg edits (or a sibling mod's UI); this is noted for the morning, not argued.

## Agreement across seats (blind → counts)
| # | Finding | Seats | Rank | Fix |
|---|---|---|---|---|
| 1 | `Hide()` is not transactional: the `HiddenTarget` is added to `_hidden` AFTER the three CanvasGroup writes (FogController.cs:201-214). If an interop setter throws mid-mutation the leaf stays invisible with no record (ShowHidden cannot restore it) and the next Hide snapshots `alpha == 0` as the "original", so a later restore writes 0 back permanently; Astra adds: fallback SetActive then runs on a still-transparent object | Kimi M, Astra M | MATERIAL (setter failure on 1.4.7 NOT PROVEN; consequence if it happens = minimap gone for the session) | Tier A: snapshot + register BEFORE mutating; roll back the snapshot on exception, then fall back |
| 2 | `HasProtectedItemUI` is a point-in-time probe at hide time. Labels/tooltips parented under the Minimap leaf AFTER the hide inherit alpha 0 / no raycasts — the 2026-09-10 incident class at leaf scope. On 1.4.7 labels live under a shared ANCESTOR, not the leaf | Astra M, Kimi M (conditional), GLM M | NOT PROVEN for 1.4.7 (all three say so); MATERIAL under a future restructure | Tier B: spawn-time / reparent guard (design needed; no cheap version without a per-frame subtree scan) |
| 3 | `_hidden` accumulates dead entries across BLIND→BLIND zone changes (cleared only by ShowHidden); the per-frame `IsHidden` scan in BLIND is O(n) over them | Gemini M, GLM MINOR, Astra, Kimi | MINOR (bounded by zones per session; Il2Cpp handles held) | Tier A: prune fake-null entries when Hide registers a new target |
| 4 | Stale invariant in comments: "a hidden minimap receives no Updates … costs nothing in BLIND" is true only for the SetActive fallback; the CanvasGroup path keeps the GameObject active so the Update postfix fires every frame in BLIND (FogController.cs:56-61; Patches.cs:47-52) | Kimi MINOR, Astra | MINOR | Tier A: comment rewrite |
| 5 | No latch on two warnings that can repeat every frame in BLIND: the `HasProtectedItemUI` refusal (FogController.cs:166 — no record is created so Update → Apply → Hide → warn every frame) and "apply failed" (128, e.g. a renamed `RevealRadius`) | Astra M, GLM MINOR | MATERIAL under a game patch (log spam + two subtree probes per frame); nothing on 1.4.7 | Tier A: latch both once per session (the per-frame probe itself is Tier B) |
| 6 | Silent `catch { }` around the zone-default capture (FogController.cs:74-90): a broken `RevealRadius` getter leaves zero log lines; LIMITED/SCOUT silently scale off the 150 fallback | Kimi MINOR, Astra ("capture exceptions are silent") | MINOR | Tier A: one latched warning |
| 7 | `Prefs.Save` swallows everything (Prefs.cs:30): a failed save silently reverts the level at next launch | Kimi MINOR, Astra MINOR, GLM | MINOR | Tier A: one latched warning |
| 8 | Settings UI dead on 1.4.7: template `Toogle - Minion Health Bars` / anchor / `GetChild(0).GetChild(0)` root (NativeSettings.cs:31-33,46-58). Degrades cleanly (one latched warning, cfg works) but the mod's identity feature is gone for every 1.4.7 player; same failure in Terrible Tooltips and Terrible Inventory | all four | MATERIAL | Tier A: diagnostic — when the probe fails, the one warning lists the settings root's child names so the real 1.4.7 name can be read from the log. Tier B: the actual adapter repair (shared with the siblings; needs the hierarchy) |
| 9 | CanvasGroup-restore path never calls `SetActive(true)`: if the GAME deactivates the leaf while BLIND owns a CanvasGroup on it, leaving BLIND leaves it inactive | GLM M (NOT PROVEN in 1.4.7) vs Astra ("existing CanvasGroup targets retain game-owned activation state on exit" — listed as correct) | DISAGREEMENT → Tier B | see below |
| 10 | Radius provenance is global: `_writtenRadii` cap 32 can forget a mod-written value that a pooled instance later re-presents as a "default"; a genuine default equal to an earlier written value is rejected | Astra M, Kimi MINOR | NOT PROVEN to occur (needs >32 distinct radii or a coincidence) | Tier B |
| 11 | `_shutdown`/`_restoring` guards cover only the Update path; Awake and direct Apply can still write during teardown | Astra M (reentrant Awake NOT PROVEN) | NOT PROVEN | Tier C |
| 12 | Listener delegates handed to Il2Cpp UnityEvents are not pinned (NativeSettings.cs:192,198); Terrible Tooltips 3.0.2 keeps a `_keepAlive` list for exactly this | Kimi NOT PROVEN | NOT PROVEN (Il2CppInterop likely roots them) | Tier B (3 lines, but rows do not build on 1.4.7 anyway) |
| 13 | SPEC.md rule 4 still specifies the OLD "outermost ancestor named minimap" law; code is leaf-only since v1.0.1 | all four | MINOR (the contract lies) | Tier A: doc |
| 14 | README: "MelonLoader 0.6.x" (box runs 0.7.2 Open-Beta); file map omits the Minimap.Update patch; no word that the settings rows are unavailable on 1.4.7 | all four | MINOR | Tier A: doc |
| 15 | `CreateEnumDropdown` unused by fog (78 lines) | Gemini remove, Astra retire, Kimi KEEP (documented family reference), GLM keep + mark | DISAGREEMENT | Tier B — untouched tonight |
| 16 | `<Compile Remove="docs\**\*.cs">` is NOT needed here: `docs\` is a sibling of the project directory, so the SDK glob never sees the baselines | Kimi, Astra | correct | Tier A adds it anyway as an inert guard (overnight rule for the three mods); documented as inert |
| 17 | csproj deploys to the live game by default (CopyToMods unless DeployToMods=false); absolute single-machine paths | Kimi MINOR, Astra | MINOR (boss's chosen workflow) | Tier B: boss decides opt-in |
| 18 | BLIND with the leaf-only hide: does the minimap FRAME remain? | all four carry the 02b NOT PROVEN | NOT PROVEN | in-hand (MORNING-README step 2) |
| 19 | Misspelled / out-of-range `VisionLevel` in the cfg: Gemini says falls back to NORMAL; GLM says if the parse throws, `Prefs.Init` throws and the mod fails to load — depends on MelonLoader's enum mapper | Gemini vs GLM | NOT PROVEN | Tier B: verify against MelonLoader 0.7.2 |

Everything else: all four seats agree that on the 1.4.7 hierarchy no vision level touches ground labels, tooltips or HUD (leaf-only hide + protected probe + `activeSelf` adoption guard); leaving BLIND restores without a live minimap (loading-screen safe); BLIND→BLIND overlap is tracked per target; CanvasGroup and SetActive never both apply to one target on the success path (finding 1 is the failure path); deferred `Destroy` of an added CanvasGroup is safe; Terrible Tooltips shares no Harmony target and no object with this mod after the leaf-only fix. Single-rename takedowns are all loud (one warning per patch class).

## Disagreements — named, not smoothed
- **Finding 9 (restore vs game-parked leaf).** GLM: MATERIAL, add `SetActive(true)` in the CanvasGroup restore path. Astra: the current behaviour is the correct one — the mod restores only what it changed (alpha/raycasts/interactable) and leaves activation to the game. Conductor: Astra's reading matches the mod's own law ("never adopt something the mod didn't hide", FogController.cs:185-188) and the v1.0.0 fable-pass fix "leaving BLIND can no longer resurrect a game-parked minimap". Tier A does not touch it; Tier B records GLM's sequence for an in-hand check (BLIND → cutscene → leave BLIND).
- **Finding 15 (`CreateEnumDropdown`).** Two seats delete, two keep. Conductor: keep; it is documented as the family reference (NativeSettings.cs:205-209) and deleting code the siblings copy from is not a night decision.
- **Finding 16 (`Compile Remove`).** Kimi and Astra prove it is inert here. Conductor: added anyway per the overnight rule so the three mods carry the same line; the ticket says it is inert.
- **Verdicts.** Astra: REBUILD FogController (ownership/state bookkeeping); Gemini, Kimi, GLM: PATCH everything. Conductor: Tier A is PATCH-only; Astra's ownership arguments (findings 2, 10, 11) are Tier B/C and recorded.
- **Settings rows tonight.** Gemini's Q4 reads as if clicks work; Astra says NOT PROVEN they ever built. The logs settle it: the probe failed in every session (BRIEF fact c). Not a code disagreement — a fact the morning note must carry.

## Tier A → v1.0.1 (extend the existing unreleased 1.0.1; one builder ticket, one cross-vendor review, one in-hand pass) — ..\build-2026-09-11\TICKET-A.md
Findings 1, 3, 4, 5, 6, 7, 8 (diagnostic only), 13, 14, 16. All cheap, agreed by ≥2 seats or comment/doc-only. The player-visible change is only in failure paths (a partial hide now rolls back; warnings latch; a failed save is reported). Nothing changes what BLIND/HARD/…/ORACLE do on the success path.
## Tier B (needs a decision or an in-game test)
2 (reparent guard design), 8 (the real settings adapter for 1.4.7 — after the diagnostic reveals the new names; shared with Tooltips/Inventory), 9 (GLM's sequence in-hand), 10, 12, 15, 17, 18 (frame check), 19 (verify MelonLoader enum parse).
## Tier C (rebuilds)
11 (mutation guards on every entry + explicit patch/callback teardown), Astra's per-lifetime radius provenance and ownership-tracked restore, the vestigial `IsChildOf` clause (Kimi MINOR, harmless), `RestoreDefaults` restoring only the last instance's radius, Settings toggle references never torn down.
