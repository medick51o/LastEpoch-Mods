# 🟣➤🔷 GLM — glm-5.2-high via Cursor (💸 credits: 41,217 in / 3,341 out) · adversarial player-harm lens · blind, read-only · 2026-09-11
sessionId 777491bf-7049-4b3b-9a7a-98da130d6f35 · packet C:\Sync\Projects\fog-review-2026-09-11 @ a36542f · verbatim below

DONE_WITH_CONCERNS

| rank | claim | failure mechanism | verify-by / repair | path:line |
|---|---|---|---|---|
| MATERIAL | HasProtectedItemUI only runs at hide time; a future game update that parents `GroundItemLabel` under `minimap.gameObject` makes BLIND hide post-spawn labels via CanvasGroup alpha=0 + blocksRaycasts=false → loot invisible AND unpickable (same incident class, leaf path) | `GetComponentInChildren<GroundItemLabel>(true)` is null at zone-in when no items are dropped; check passes; CanvasGroup applied; labels spawned later inherit alpha=0 | In-hand on a hypothetical future build, OR add a spawn-time guard / reparent-watch. Current 1.4.7 is safe (labels share an ANCESTOR, not the leaf) — NOT PROVEN for today | FogController.cs:155-168, 240-250 |
| MATERIAL | `ShowHidden` CanvasGroup-restore path never `SetActive(true)`: if the game deactivates `minimap.gameObject` while BLIND owns a CanvasGroup on it, leaving BLIND restores alpha=1 but leaves the GO inactive → minimap gone after BLIND | Restore branch only writes alpha/blocksRaycasts/interactable + Destroy(added group); no `SetActive(true)` | Walk: BLIND→cutscene deactivates minimap GO→leave BLIND→ShowHidden at 308-328. Requires game to deactivate the leaf; LE cutscenes tend to deactivate ancestors, not the leaf — NOT PROVEN in 1.4.7 | FogController.cs:318-326 |
| MINOR | BLIND→BLIND zone changes accumulate dead `HiddenTarget` entries in `_hidden` until leaving BLIND; per-frame `IsHidden` is O(n) over that list | Each zone-in adds one entry; old zone's GO is destroyed but entry remains; `IsHidden` iterates all on every Update postfix frame in BLIND | Bounded by zones-per-session; clear on level change. Optional: prune nulls in `IsHidden`/`ShowHidden` | FogController.cs:263-285, 308-333 |
| MINOR | `Minimap.Update` rename by EHG silently disables BLIND re-assert; BLIND still hides on Awake but cannot recover from overlay-map close / cutscene re-activation → minimap pops back mid-BLIND | `TryPatch` catches the registration failure and logs a warning; `OnMinimapUpdate` never runs | Loud warning at startup; player-visible: minimap reappears. Not loot-breaking | FogOfWarMod.Patches.cs:21-34, FogController.cs:55-69 |
| MINOR | `RevealRadius` field rename makes Apply throw on every Awake/Update; caught and warned; radius levels (LIMITED/SCOUT/ORACLE/HARD) silently inert, BLIND hide still works | `m.RevealRadius = radius` throws; catch at 124-128 logs "apply failed" each call → log spam per frame in BLIND | Loud (per-frame warning). Player harm: dial appears dead for non-BLIND levels | FogController.cs:113-128 |
| NOT PROVEN | Terrible Tooltips / LastEpoch_Hud reparenting minimap or labels could fight the re-assert loop | Both mods toggling active/alpha on the same GO | No mechanism traced in this packet | — |
| NOT PROVEN | Out-of-range/misspelled `VisionLevel` in cfg: MelonLoader enum parse behavior unverified here | If parse throws, `Prefs.Init` throws, `OnInitializeMelon` throws → mod fails to load; if it defaults, silent fallback to NORMAL | Verify against MelonLoader 0.7.2 `MelonPreferences` parse path | Prefs.cs:18-25 |

Q1 — Single renames: `Minimap.Awake`→fog control dead (loud, TryPatch catch); `Minimap.Update`→BLIND re-assert dead (loud, minimap can resurface mid-BLIND); `RevealRadius`→radius inert + per-frame warnings; `SettingsPanelTabNavigable.Awake`→settings UI dead (loud, cfg works); `Toogle - Minion Health Bars` / `Header - Interface` / `GetChild(0).GetChild(0)`→settings degrade (one latched warning, fog control intact). Terrible Tooltips patches the same label/tooltip types `HasProtectedItemUI` probes; if TT reparents labels UNDER `minimap.gameObject`, BLIND refuses to hide (degrades to HARD) — safe direction, no fight. LastEpoch_Hud touching the minimap GO could race the Update re-assert — NOT PROVEN.

Q2 — Non-BLIND levels only write `RevealRadius`, never hide anything. BLIND hides `minimap.gameObject` only, guarded by `HasProtectedItemUI`. Leaving BLIND via `SelectLevel`→`Apply`→`ShowHidden` works; restore is not gated on minimap aliveness (loading-screen safe). BLIND→BLIND zone change: new zone's minimap hides independently; old dead entries skipped via `go==null`. Pooled/parked minimap: `if (!target.activeSelf) return` (FogController.cs:188) prevents adopting a parked GO; CanvasGroup-restore path doesn't `SetActive(true)`, so a parked GO is not resurrected. CanvasGroup AND SetActive on one target: Hide tries CanvasGroup first, falls back to SetActive only on exception — one mechanism per target, but the CanvasGroup path does NOT track/restore the GO's active state (see MATERIAL row). `HasProtectedItemUI` passes wrongly when labels aren't spawned yet (true in 1.4.7, harmless because labels live under a shared ancestor, not the leaf); refuses wrongly only if the leaf legitimately contains a `GroundItemLabel` (NOT PROVEN for 1.4.7).

Q3 — `OnMinimapUpdate`: non-BLIND exits at `HidesMinimap` (one enum compare); BLIND-hidden exits at `IsHidden` (O(n) over `_hidden`, n = BLIND zones this session — small). `_hidden` growth: +1 per BLIND zone, cleared on leaving BLIND, no unbounded leak. `_writtenRadii` capped at 32 with `RemoveAt(0)` (O(32)). Il2CPP handles: `_minimap` overwritten per Awake; `_hidden` holds destroyed GO refs until clear — Il2CPP GC handles, no leak. `Destroy(addedCanvasGroup)` once per restore — fine.

Q4 — Template missing → one latched warning, no injection, cfg works (NativeSettings.cs:38-58). Level change is live: `SettingsUi.SelectLevel`→`Prefs.Save`+`FogController.Apply`+`SyncLegend` (SettingsUi.cs:69-77); `Apply` writes the live `_minimap` if alive, else next `OnMinimapAwake` applies it. Out-of-range/misspelled `VisionLevel`: behavior depends on MelonLoader's enum parse — NOT PROVEN. `Prefs.Save` swallows errors silently (Prefs.cs:29); autoload reads on init — a corrupt cfg falls back to NORMAL silently (player's BLIND becomes NORMAL on next launch).

Q5 — Top-3: (1) SPEC.md rule 4 still describes the OLD "outermost ancestor named minimap" law, directly contradicting the new leaf-only law in FogController.cs:152-157 — stale contract, must update. (2) README.md "Install MelonLoader 0.6.x" while the boss's box runs 0.7.2 Open-Beta — stale. (3) `NativeSettings.CreateEnumDropdown` is unused by fog (retained as family reference); acceptable but worth a `#if FAMILY_REFERENCE` guard or move to a shared lib — MINOR.

Q6 — FogController.cs: PATCH (spawn-time guard for future label reparenting; `SetActive(true)` in CanvasGroup restore path). FogOfWarMod.cs: ok. FogOfWarMod.Patches.cs: ok. FogLevels.cs: ok. SettingsUi.cs: ok. NativeSettings.cs: PATCH (mark `CreateEnumDropdown` as retained reference, or move out). Prefs.cs: PATCH (don't silently swallow save failure — log once). BuildInfo.cs: ok. csproj: ok. README.md: PATCH (0.6.x→0.7.x). SPEC.md: PATCH (rule 4 → leaf-only law). CHANGELOG.md: ok.

MANIFEST — Sandbox shell returned no output to `md5sum`/`echo` (tool reports "no exit status"), so I CANNOT hash files in this environment; no values invented. Files read: BRIEF.md, medick_FogOfWar/src/{FogController,FogOfWarMod,FogOfWarMod.Patches,FogLevels,SettingsUi,NativeSettings,Prefs,BuildInfo}.cs, medick_FogOfWar/medick_FogOfWar.csproj, README.md, SPEC.md, CHANGELOG.md, docs/build-2026-09-10/{TICKET-02-blind-hide-scope,TICKET-02b-repair-leaf-only,REVIEW-ticket02-gemini-REJECT,REVIEW-ticket02b-gemini,baseline.md5}.md, DELTA-ticket02b.patch, baseline/{BuildInfo,CHANGELOG,FogController}.cs, baseline/medick_FogOfWar.csproj.

🟣➤🔷 GLM
