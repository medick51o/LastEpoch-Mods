# 🟣➤🌙 Kimi — kimi-k3-high via Cursor (💸 credits: 46,313 in / 12,350 out) · code-quality lens · blind, read-only · 2026-09-11
sessionId 0bb5ca45-95a5-400c-ae1a-97bb635b7c04 · packet C:\Sync\Projects\fog-review-2026-09-11 @ a36542f · verbatim below

DONE_WITH_CONCERNS

**FINDINGS (rank · claim · failure mechanism · verify-by / repair · path:line)**

1. MATERIAL · Hide() can leave the minimap invisible and untracked, then lock alpha 0 forever · `_hidden.Add` runs AFTER the alpha/raycast/interactable writes (FogController.cs:210-214); if an interop setter throws mid-mutation the outer catch warns but the leaf stays invisible with no entry, so `ShowHidden` can't restore it — and the next `Hide` re-captures `group.alpha==0` as the "original" (201-208), so a later restore writes 0 back permanently · Add the entry before mutating, roll back in catch; in-hand: BLIND→leave→check corner · medick_FogOfWar/src/FogController.cs:201-214,231
2. MATERIAL (conditional) · `HasProtectedItemUI` samples the leaf subtree once, at hide time only · labels reparented under the Minimap leaf AFTER the hide (runtime spawn, EHG restructure) are never re-checked — the Update re-assert path (55-68) doesn't probe; the 2026-09-10 incident class reproduces at leaf scope · NOT PROVEN for 1.4.7 as-shipped; grep future game updates for labels under Minimap; the probe's catch→refuse (250-255) is correctly fail-visible · FogController.cs:161-167,240-249
3. MINOR · stale cost narration: "a hidden minimap receives no Updates" · only true for the SetActive fallback; the CanvasGroup path keeps the GameObject active, so in BLIND the Update postfix fires every frame and runs O(_hidden) `IsHidden` · fix comments; cost is small but the stated invariant is false · FogController.cs:60-61, src/FogOfWarMod.Patches.cs:50-51
4. MINOR · vestigial `IsChildOf` in `IsHidden` · target is always `minimap.gameObject` (leaf-only), so `minimap.transform == target.transform` for any live entry; the clause is dead logic from the ancestor era · delete clause · FogController.cs:270-273
5. MINOR · silent `catch {}` on Awake capture hides real interop faults · a broken `RevealRadius` getter (game update) fails with zero log lines even with DebugLog=true; only symptom is LIMITED/SCOUT scaling off the 150 fallback · log once per session · FogController.cs:74-90, FogLevels.cs:19
6. MINOR · `Prefs.Save` swallows everything · failed save (disk/AV) silently reverts the level next launch; with `autoload:true`, a hand-edited cfg is clobbered by click-save/quit-save · warn once on save failure · src/Prefs.cs:23,28-31
7. MINOR · `_writtenRadii` cap can forget · after 32 distinct mod-written radii the oldest is dropped; a pooled instance re-presenting that forgotten value at Awake is adopted as a "zone default" — the compounding guard reopens · needs >32 distinct radii in one session; raise cap or key by value+instance · FogController.cs:36,120-123
8. MINOR · csproj is single-machine · hardcoded absolute `$(ML)`/`$(GM)` and a CopyToMods target that deploys to the live game BY DEFAULT on every build · parameterize via env/props; flip default to opt-in · medick_FogOfWar/medick_FogOfWar.csproj:15-16,71-74
9. MINOR · SPEC.md rule 4 contradicts the code · still specifies "outermost ancestor whose name contains minimap"; the law since v1.0.1 is leaf-only · rewrite rule 4 · SPEC.md:35-41 vs FogController.cs:147-152
10. MINOR · README stale · install step says MelonLoader 0.6.x (box runs 0.7.2); file map omits the Minimap.Update patch · README.md:26,51
11. NOT PROVEN · managed delegates on Il2Cpp UnityEvents aren't pinned · `AddListener(new Action<bool>(...))` + SelectLevel closures aren't held in a static; if the interop wrapper doesn't root them, GC silently kills row clicks mid-session (ML 0.7.2's Il2CppInterop likely roots; cannot prove from source) · 3-line static-list pin · src/NativeSettings.cs:192,198, src/SettingsUi.cs:57
12. NOT PROVEN · leaf-only hide vs minimap frame · if the frame renders outside the leaf, BLIND leaves an empty frame (carried from Gemini 02b) · in-hand BLIND corner look · FogController.cs:159
13. NOT PROVEN (negative) · docs baseline *.cs compile hazard: none · `docs/` is a SIBLING of the project dir; SDK globbing under `medick_FogOfWar/` never sees `baseline/FogController.cs` — no `Compile Remove` needed · csproj has no Compile items · medick_FogOfWar.csproj (whole)

**Q1** — Single-rename takedowns, all loud (one warning each, Patches.cs:24-35): `Minimap.Awake`→no zone capture/apply (clicks still apply live); `Minimap.Update`→BLIND can pop back after overlay/cutscene; `SettingsPanelTabNavigable.Awake`→settings UI dead, cfg-only. `RevealRadius` rename = compile break; stale binary fails SILENTLY on the getter (finding 5). Template `"Toogle - Minion Health Bars"`/anchor/root path is ALREADY broken on 1.4.7 (fact c): one shared template name takes this mod's AND both Terrible siblings' settings UIs down together (NativeSettings.cs:32-33,46-65). Terrible Tooltips: no object conflict post-02b — fog's scope is the Minimap leaf, TT owns labels/tooltips; disjoint. Any mod writing alpha/active on the same leaf fights the per-frame re-assert last-writer-wins (NOT PROVEN which installed mod would).

**Q2** — On 1.4.7's hierarchy no level touches labels/tooltips/HUD: leaf-only + protected probe + activeSelf adoption guard. Leaving BLIND restores regardless of minimap aliveness (FogController.cs:100-104); dead/zone-stranded entries are fake-null-skipped and the list cleared (312-331); BLIND→BLIND overlap tracked per target (171-183). CanvasGroup and SetActive never both apply to one target (fallback only after rollback, 219-229). Wrong-refuse: probe exception→degrade to HARD, loud. Wrong-pass: finding 2 (point-in-time). Restore wrongness: finding 1.

**Q3** — Outside BLIND: 3 cheap checks/frame. Inside BLIND (CanvasGroup): `IsHidden` O(n) every frame — finding 3. `_hidden` grows ~1/zone during BLIND, compacted only at ShowHidden. `_writtenRadii` bounded (finding 7). Il2Cpp handles across zones are all fake-null-checked in try/catch. Deferred `Destroy` of the added CanvasGroup (320) is safe: ShowHidden only runs on non-BLIND levels, so no same-frame re-hide (100-104).

**Q4** — Root path `GetChild(0).GetChild(0)`+template+anchor probed once (NativeSettings.cs:46-65); missing → one latched warning, no injection, cfg works. Live path: row click→SelectLevel→Save+Apply→live hide/radius (SettingsUi.cs:68-76). With templates broken, cfg edit + autoload reloads the value but NOTHING calls Apply on autoload — effect lands next zone-in (next Minimap.Awake), not instantly. Misspelled `VisionLevel`: MelonPreferences falls back to NORMAL with its own warning; even an out-of-range cast hits `_ => def` and `HidesMinimap==false` → NORMAL behaviour (FogLevels.cs:32,37-38). Save loss: finding 6.

**Q5** — Top 3: (1) SPEC rule 4 rewrite to leaf-only (the contract currently lies). (2) Stale "receives no Updates" comments (finding 3). (3) README 0.6.x + patch-list omission. `CreateEnumDropdown`: KEEP — deliberately retained family reference (NativeSettings.cs:205-209), not dead weight. `Compile Remove`: not needed (finding 13).

**Q6** — FogController.cs PATCH (finding 1 ordering + comments). FogOfWarMod.Patches.cs PATCH (comment). SettingsUi.cs PATCH (pin delegates). NativeSettings.cs PATCH (pin delegates; keep dropdown). Prefs.cs PATCH (log save failure once). FogOfWarMod.cs, FogLevels.cs, BuildInfo.cs — keep, no changes. csproj PATCH (finding 8). SPEC/README PATCH (findings 9-10); CHANGELOG accurate.

**MANIFEST** — My sandbox shell returned no output/exit status on three `md5sum` attempts, so I cannot hash; per instructions I state this instead of inventing values. Cross-check available: baseline.md5 and Gemini's manifest cover the write set. Files read (22): BRIEF.md; README.md; SPEC.md; CHANGELOG.md; medick_FogOfWar.csproj; src/{FogOfWarMod,FogOfWarMod.Patches,FogController,FogLevels,SettingsUi,NativeSettings,Prefs,BuildInfo}.cs; docs/build-2026-09-10/{TICKET-02,TICKET-02b,REVIEW-ticket02-gemini-REJECT,REVIEW-ticket02b-gemini,DELTA-ticket02,DELTA-ticket02b,baseline.md5} + baseline/{BuildInfo.cs,CHANGELOG.md,FogController.cs,medick_FogOfWar.csproj}.

🟣➤🌙 Kimi
