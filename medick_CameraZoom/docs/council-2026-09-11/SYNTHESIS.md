# Terrible Zoom v1.0.0 — council synthesis (2026-09-11, overnight auto mode)
Seats (blind, read-only, packet C:\Sync\Projects\zoom-review-2026-09-11, mirror commit 806b1c1): 🔵 Astra (gpt-6-astra, architecture) · 🟢 Gemini (Antigravity, robustness / player experience; brain UNREPORTED) · 🟣➤🌙 Kimi k3-high (code quality, 💸) · 🟣➤🔷 GLM 5.2-high (adversarial correctness, 💸). ⚫ Grok benched. All four returned DONE_WITH_CONCERNS. Mirror `git status --porcelain` empty after all four seats; repo unchanged (only the new docs\ folder is untracked).
Provenance: NO gap. The DLL in the game's Mods folder (24,064 B, 2026-07-01 21:00, md5 14c5806947db8a1a4321ffa746d8069e) is byte-identical to the repo's `bin\Release\net6.0` build of this source. A fresh build of the same source in the mirror is green (0 warnings, 0 errors; same size, different md5 only because the build is not deterministic). Astra and Gemini computed MD5s that match the mirror for every file; Kimi and GLM could not run a shell in the Cursor read-only sandbox and listed files without hashes (both said so rather than fabricate).
Astra additionally inspected Il2CppLE.dll (md5 5FEF0752…) and confirmed `CameraManager.instance`, all nine camera properties as read/write Single, `Start()`, `resetZoom()`, `EpochInputManager.instance` and `forceDisableInput` (Boolean). Conductor additionally confirmed in the same stub that `EpochInputManager` exposes `IsInputFieldActive` (property) and `InputFieldActive()` (method) — the game's own "player is typing" flag.

## Agreement across seats (blind → counts)
| # | Finding | Seats | Rank | Fix |
|---|---|---|---|---|
| 1 | InputGuard releases a lock it does not own: `_applied = true` is set on every wanted frame even when the flag was already true (Terrible Cooldowns), and close writes `false` unconditionally (InputGuard.cs:34-39). Mirror image of the Cooldowns finding fixed (staged) earlier tonight | GLM BLOCKER, Astra M, Gemini M, Kimi M | MATERIAL (Cooldowns is installed on the boss's box) | set `_applied` only when this mod flipped the flag; rewrite header comment |
| 2 | The mod's zoom model contradicts the game's numbers. Code assumes the zoom-out limit `zoomMin` sits below `zoomDefault` ("more negative = further out", Prefs.cs:15,34; reclamp `tgt < zoomMin → tgt = zoomMin`, CameraState.cs:184-190). The game reported zoomMin -7.0 ABOVE zoomDefault -17.5. On paper: with the boss's live ZoomMin = -1.0 the reclamp pins `targetZoom` to -1 every frame the panel is closed; after Rescue (Prefs.ZoomMin := -7) it pins to -7, not the game default -17.5 | GLM BLOCKER ("inverted; player can never zoom out"), Astra M ("reclamp rejects the captured default; direction NOT PROVEN"), Gemini MINOR ("traps camera at close-up, recoverable"), Kimi ("coherent; -1 is a config oddity") | NOT PROVEN which way is "out"; the numeric contradiction IS proven | Tier B: 2-minute in-hand direction test (see MORNING-README), then flip labels/reclamp or document. No Tier A change to camera behaviour |
| 3 | NaN / Infinity in PanelX/PanelY/MenuScale survive `Mathf.Clamp`; NaN rect → panel invisible, Reset unreachable, NaN re-persisted on close (SettingsPanel.cs:43-45,55-56,97-98). Camera prefs are already `Sane()`-guarded | Astra M, Kimi M, Gemini MINOR, GLM "invisible, not a crash" | MATERIAL (unrecoverable without cfg surgery) | sanitize on Init: non-finite → default, one warning line |
| 4 | End toggles the panel (and engages the input lock) while the player is typing in chat/search: raw `Input.GetKeyDown(KeyCode.End)` with no focus check (CameraZoomMod.cs:26) | Gemini M, Kimi M, GLM M, Astra MINOR | MATERIAL | gate on `EpochInputManager.instance.IsInputFieldActive` (verified in stub) |
| 5 | `CameraManager.instance` getter failure is swallowed by OnUpdate's bare catch, so the 300-frame stall warning can never fire for that path — silent idle after a game patch (CameraZoomMod.cs:38-45) | Kimi M, Astra ("singleton failures bypass capture diagnostics") | MINOR (diagnostic gap, not a player-visible defect today) | count consecutive OnUpdate exceptions; warn once at 300 |
| 6 | Stale narration: "v2 safety rules" / "v1.x's bug class" in a v1.0.0 mod (CameraState.cs:15,96,106); Prefs descriptions claim game defaults ≈ -15 / 1-2 / 5-8 vs tonight's captured -7.0 / 0.2 / 2.5 (Prefs.cs:34-36); README install line says MelonLoader 0.6.x vs 0.7.2 tonight (README.md:30) | Kimi, GLM, Astra (Gemini silent) | MINOR | comment/doc-only rewrite; do NOT touch the disputed "more negative = further out" wording (finding 2) |
| 7 | Orphan cfg keys FOV, MinFOV, MaxFOV, SmoothSpeed, RmbModifier (pre-Terrible v1.x) and a separate `[kg_CameraZoom]` section from a different mod — unregistered, inert | all four (GLM ranks MATERIAL as "misleading", others harmless) | MINOR (harmless) | document as inert in CHANGELOG; no code deletes them; the kg section belongs to another mod |
| 8 | csproj has no `<Version>`; version literal lives only in BuildInfo.cs; no `<Compile Remove>` for docs | GLM, Kimi (Astra: "none establishes a runtime defect") | MINOR | add `<Version>`, docs compile-exclude; NO copy-to-Mods target (none exists) |
| 9 | Per-field failure inside one try/catch: a single renamed CameraManager member aborts the whole Apply block every frame (silent idle + per-frame exception); `resetZoom()` rename silently no-ops Rescue; `_wroteLock` set only after all three angle setters succeed, so a partial write escapes unlock cleanup | GLM M, Astra M | MATERIAL under a future game patch; NOT a defect on 1.4.7 | Tier B/C: capability preflight, per-field ownership, one fault line |
| 10 | Capture ownership not enforced at write boundaries: `Captured` stays true if `GetInstanceID()` throws for a new manager; Restore/UI accept any manager; a reused manager whose zone defaults change is never recaptured | Astra M, Kimi ("harmless in practice, mod is unloading") | NOT PROVEN to occur on 1.4.7 | Tier B |
| 11 | Compare-then-write does not arbitrate with a second camera mod; captured "originals" may already include another mod's writes | Astra M, Kimi NOT PROVEN (no second writer installed; kg_CameraZoom.dll absent) | NOT PROVEN | Tier C |
| 12 | OnGUI re-reads `CameraManager.instance` + 3 fields per GUI event (2-4×/frame while open); interpolated strings per event | GLM MINOR, Gemini MINOR, Astra, Kimi ("acceptable for IMGUI") | MINOR | Tier C |
| 13 | Theme: retry loop after a partial `Ensure` failure leaks 3×3 textures; no teardown on unload; style dictionaries uncapped (bounded in practice by the clamped scale) | Kimi MINOR, Astra MINOR, GLM MINOR | MINOR | Tier C |
| 14 | `GUIUtility.hotControl` read inside CameraState.Apply couples camera logic to IMGUI state | Kimi MINOR | MINOR | Tier C |
| 15 | Panel taller than a small display at Menu scale 2.0; no keyboard position reset | Astra MINOR | MINOR | Tier B |

Everything else: all four seats confirm the settings panel is self-drawn IMGUI with NO native template lookup — the "Toogle - Minion Health Bars" failure that killed the sibling mods on 1.4.7 does not apply here. All four confirm no NaN can reach a camera write (Sane/Finite gates + slider NaN check), the lock→unlock restore is one-shot and per-manager (no tilt fight, no stale lock across zones), no Il2Cpp object reference is held past frame scope, and no unbounded growth is proven.

## Disagreements — named, not smoothed
- **Zoom direction (finding 2).** GLM: BLOCKER, the reclamp is inverted. Kimi: coherent, -1 is a config oddity. Astra: the numeric contradiction is real, the direction is NOT PROVEN without native code. Gemini: MINOR, recoverable. Conductor: the boss plays daily with ZoomMin = -1.0 and saved prefs tonight without complaint, which means the pinned -1 view is the one he wants — evidence (not proof) that in this game a HIGHER value is further out and the mod's label is backwards. Any change here alters what the boss sees every session, so it is Tier B behind a two-minute in-hand test, not Tier A. Nothing in Tier A touches the reclamp, the sign, the slider range, or the "more negative" wording.
- **InputGuard rank.** GLM BLOCKER vs three MATERIAL. GLM's mechanism also misreads the staged Cooldowns fix (it still re-asserts every wanted frame). Conductor: MATERIAL; the fix is identical to Cooldowns' and ships in Tier A.
- **Orphan keys.** GLM MATERIAL ("misleading"), three seats harmless. Conductor: MINOR, document only; the `[kg_CameraZoom]` section is another mod's and is not ours to delete.
- **Verdicts.** Astra: REBUILD CameraState + InputGuard (ownership/lease architecture); GLM: REBUILD CameraState; Kimi and Gemini: PATCH everything. Conductor: Tier A is PATCH-only; the REBUILD arguments (per-field ownership, lease service shared with Cooldowns) are Tier C and recorded.
- **Copy-to-Mods target.** GLM and Kimi say add `<Version>`; nobody asked for a copy target and none exists. Conductor: no target is added; tonight's law is nothing deploys.

## Tier A → v1.0.1 (one builder ticket, one cross-vendor review, one in-hand pass) — ..\build-2026-09-11\TICKET-A.md
Findings 1, 3, 4, 5, 6 (doc/comment only, excluding the disputed direction wording), 7 (document only), 8. All cheap, agreed by ≥2 seats or comment/doc-only, none changes what the camera does; the player-visible changes are (a) End no longer fires while typing and (b) the panel becomes recoverable from a bad cfg.
## Tier B (needs a decision or an in-game test)
2 (direction test → flip or document; then decide the ZoomMinHi cap and the Rescue-pins-to-zoomMin behaviour), 9 (capability preflight + one fault line — worth doing before the next game patch), 10, 15.
## Tier C (rebuilds)
11 (camera-owner arbitration), 12 (per-frame OnGUI cache), 13 (Theme teardown/retry), 14 (hotControl decoupling), and the cross-mod input-lock ownership service shared with Terrible Cooldowns (Astra's real fix for finding 1; Tier A's `_applied` correction is the cheap version, now symmetric on both mods).
