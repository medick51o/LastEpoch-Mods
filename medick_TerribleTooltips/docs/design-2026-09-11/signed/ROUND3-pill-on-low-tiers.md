# Round three (08:47) — "Given R2, is the pill still needed on T1–T5?" — four Cursor seats, verbatim
Question put to each: Andrew ruled D2 pill + GREATER-AFFIX TINT (#C990FF on T6/T7 sentences, T1–T5 white, pill ink on the palette). Is the pill still needed on T1–T5 lines, or does white text plus a small coloured `Tier N · G` mark suffice, pill reserved for T6/T7? Plus: does #C990FF read apart from Tier 5 ink #A807FF and Tier 7 ink #FF44FF?

## 🟣➤⚫ Cursor-Grok — ♾️ INCLUDED · 1849 in / 2419 out — PILLS EVERYWHERE (uniform first)
Pills stay on T1–T5. R2 already bleached those sentences; the pill is now the only glanceable `N | G`. Reserving it for T6/T7 flattens T5 into T1, and mixed rows either rag the left edge or keep an empty gutter — you paid for the slot on every row anyway. Probe D2 as **uniform pills first**; dual-mode is a follow-up, not the first donor experiment.

Strongest counter: fewer clones, weaver T3 + purple bar without a second widget, T6/T7 as the only framed lines.

Test: one item with T3, T5, T6, T7 at 1080p, 500ms squint, then stash-compare. Pass = T5 ≠ T3 without reading, and T6 rows don't indent like a layout bug.

`#C990FF` reads apart from `#A807FF` (deep violet) and `#FF44FF` (hot pink) by value if it never enters the pill. Same-line T7 (`#FF44FF` ink + `#C990FF` words) is the clash to screenshot; deuteranopia may pinch those two magentas.

🟣➤⚫ Cursor-Grok

## 🟣➤🎼 Composer — ♾️ INCLUDED · 3261 in / 1249 out — PILL ONLY ON T6/T7
**Answer:** Yes — split the row shape. T1–T5: white sentence + coloured `Tier N · G` text mark, no pill. T6/T7: native pill + `#C990FF` sentence. R2 already demotes T1–T5 to calm white; a pill there re-adds chrome the council was trying to shed, while the greater tiers earn both tint *and* pill.

**Strongest counter:** Inconsistent anatomy — players learn "affix = pill" then common-tier rows look like implicits/sealed noise; you pay the D2 donor cost once and may as well use it everywhere.

**Settling test:** Same 4-affix belt (T5/T4/T7/T1) + weaver idol, A/B: all-pill vs pill-only-T6/T7. Five-second tier/grade scan + "which is greater?" without reading the stat name.

**#C990FF vs palette:** On the dark ground, yes — it reads as soft lavender sentence tint, clearly apart from T5 `#A807FF` (deep violet ink) and T7 `#FF44FF` (hot magenta). Risk is HDR wash making T5 *plates* muddy toward pastel; test there, not in HTML.

🟣➤🎼 Composer

## 🟣➤🌙 Kimi — 💸 CREDITS · 7476 in / 1180 out — PILL ONLY ON T6/T7
**Answer: reserve the pill for T6/T7; T1–T5 get white sentence + small coloured `Tier 3 · C` text.** R2 already makes the sentence the greater-affix carrier — stacking a pill on every line rebuilds the surface overload all seven seats diagnosed (8 plates on a 4-affix idol). Hierarchy needs one hero per line: T1–T5 whisper (one hue, small signal), T6/T7 get the full moment (tinted sentence + pill). The pill's rarity IS the signal; a pill everywhere is a pill nowhere.

**Strongest counter-argument:** a signal that changes shape by tier is two grammars — the scanning eye must learn "text means normal, pill means greater," and a mixed item (T6 + T4 lines) can read as *missing pills / a render bug* rather than intent. It also undersells the T5 epic sitting one step below greatness: same furniture as a T1.

**In-game test that settles it:** same 4-affix mixed-tier exalted rendered both ways — (a) pill on all lines, (b) pill only on T6/T7 — screenshotted SDR + HDR, then the half-second triage test: name the item's best affix before reading a word. If (b) reads as hierarchy, ship it; if it reads as broken, pill everything. Also verify the cloned pill against the weaver purple bar on a weaver line.

**#C990FF separation: yes, in my judgement — but lightness does the work, not hue.** Against T5 `#A807FF` (deep, dark, saturated violet) it's clearly apart: far lighter, desaturated lavender. Against T7 `#FF44FF` the hue gap is only ~23°, but `#C990FF` is pale and calm where `#FF44FF` is neon — value contrast carries the distinction on a dark ground. Caveat: under HDR bloom and for colour-blind users the hue axis collapses, so keep the pill as the redundant T6/T7 cue — which is exactly what the shape-split above provides. Deliberately off-palette is correct: it must never be mistaken for a tier colour.

🟣➤🌙 Kimi

## 🟣➤🔷 GLM — 💸 CREDITS · 1251 in / 2601 out — PILL ONLY ON T6/T7
**Answer:** Under R2, the pill is **not needed on T1–T5** — white sentence + a small coloured `Tier N · G` mark suffices there; reserve the pill for T6/T7. R2's "calm" intent is to cut coloured surfaces; a pill on every T1–T5 line re-adds the overload R2 removes, and for colour-blind players the pill's tier hue is redundant with the **number** (always present, always colour-independent). The pill/no-pill boundary then becomes a useful shape cue: it separates the T3-green↔T6-gold deutan/protan collapse structurally (T6 has the pill + lavender sentence; T3 has neither).

**Strongest counter-argument:** a pill-only-on-T6/T7 rule demotes T5 (epic) to plain text, which may read as a status loss for the tier players care most about, and it breaks the uniform scan column — the eye loses its anchor on T1–T5 lines, where the inline `Tier N · G` text competes with the affix name for the first-glance triage the pill was built for.

**In-game test that settles it:** render one item with T3/T5/T6/T7 lines in both variants (pill-all vs pill-T6/T7-only) under SDR and HDR, and have a deutan, protan, and tritan viewer (or simulation) identify each tier+grade at a 2-second glance. If no-pill T1–T5 lines get skipped or mis-tiered, the pill is needed everywhere; if tier ID is as fast and accurate without it, reserve the pill.

**Contrast arithmetic (on #181222, L≈0.0074):** `#C990FF` L≈0.397 → **~7.8:1** (passes AA, near AAA), more legible than `#A807FF` (3.6, fails AA normal) and `#FF44FF` (6.55). `#C990FF` vs `#A807FF` (T5 ink): separable by luminance (~2.5×) across all three dichromacies — safe. `#C990FF` vs `#FF44FF` (T7 ink): luminance close (~1.2×) and hue collapses for deutan/protan (both read light-blue) — the risky pair, rescued only by the pill being a distinct shape and the "Tier 7" number inside it.

🟣➤🔷 GLM

---
Conductor tally: 3–1 for reserving the pill to T6/T7 (Composer, Kimi, GLM) vs uniform pills (Cursor-Grok). All four agree the settling test is the same in-game A/B (pill-all vs pill-T6/T7-only, SDR + HDR, half-second triage), so the probe should carry a `PillTiers` switch (All / GreaterOnly) rather than pick. All four say #C990FF reads apart from T5 ink; GLM and Cursor-Grok flag #C990FF vs T7 ink #FF44FF as the risky pair for deutan/protan viewers, rescued by the "Tier 7" number in the pill.
