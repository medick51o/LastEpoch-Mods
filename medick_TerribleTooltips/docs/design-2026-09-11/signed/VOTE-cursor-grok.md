# 🟣➤⚫ Cursor-Grok — round-2 vote/refute (2026-09-11, unblinded, read SYNTHESIS-v2 + RENDER-VERDICT + viewed the six PNGs)
sessionId 0d9350eb-a8a8-48cb-ba51-5a8ffec8b383 · ♾️ INCLUDED (xAI · cursor-grok-4.6-high) · 24100 in / 4923 out

VIEWED PIXELS (all six `renders\design-2026-09-11-*-1x.png`). Did not substitute HTML for the ranking.

### (1) Votes

1. **NO PLATE.** D0/D1c T1 plates are grey smudges; D1b's dark box on T1/F nearly vanishes into the tooltip; D1c's 25% T5 plate is CSS softness the engine will veil. D1a is the only D1 that does not draw a quad.
2. **KEEP TIER COLOUR.** D1a's right panel (purple/blue/pink names, no plates) is the mod; the left beige sentences are a luggage-tag index. That PNG is Q2 answered in one frame.
3. **If a plate ever: DARK, and only after an in-game alpha strip.** Do not pick D1c from this render — the gate ranked it first on a browser approximation of a law it cannot show (`TooltipRecolor.cs` 822–828).
4. **"Tier 5" spelled out; `T5` only as the wrap fallback.** D1b's compact codes look like item IDs; SPEC's line is "Tier N" spelled out.
5. **GRADE colour.** A beige `B` is how you hide the second axis; `T5·B` colliding to one purple is a palette fact (`Colors.cs` 34, 57), not a reason to bleach the letter. Digit + letter still parse.
6. **NO ★.** D1c's `S ★` is a sixth signal; mythic pink `S` already screams; glyph is font-dependent.
7. **120% bold gold.** `FilterRuleTooltip.cs:118` `<size=200%>` — every PNG's `RULE#69` is a watermark next to `LEVEL 63`.
8. **D1 only for 3.1.0.** D2 donor probe after Andrew has no-plate in his hands; D3 parked.

### (2) Refute

- **Render gate ranking `D1c > D1a > D1b` is not in-game evidence.** Those PNGs draw CSS plates *behind* padded text; the renderer notes themselves admit TMP overdraw is square paddingless quads *over* glyphs. D1c looking "soft and scannable" here is the June-mockup failure mode (synthesis agreement #9) repeating. T1 plates in the D1c belt are already barely there; HDR will not make a 25% `#DADADA` quad more honest.
- **D1c idol hides F** (`Tier 1` with no letter on the two T1 lines). Synthesis already parks "hide F"; the gate still called D1c calmest-clear. A colour-blind miss cannot win a scan ranking.
- **Verdict's D1b "plate only behind the tier abbreviation"** is wrong even as HTML (conductor note: one `<mark>` around `T5 · A`). Pixels: the dark box reads as a unit around the compact signal, then a beige sentence. Don't use that verdict sentence in the deck.
- **Agreement #1 overcounts "three signals."** Two plates + coloured name is overload. One coloured name + `Tier 5 · A` in colour is the product (ARCHAEOLOGY's capitalised direction). Treating name colour as the same sin as two `<mark>` quads is how D1a's left panel gets laundered into "unanimous cleaner."
- **"Unanimous D1 ships first" / "D4 IS a D1 shape"** is architecturally true (TMP tags, no widgets) and product-false if 3.1.0 ships D1a-left (beige). My 3.1.0 was no plate + **keep `TierColor`**. Do not count that as a vote for Astra's default-neutral.
- **GLM's T5 3.6:1 contrast** argues the *palette* is dim on purple, not that names should go beige. Veiling `#A807FF` under a mark made it worse; bleaching the sentence throws out the retina to paper over luminance. If T5 fails WCAG, fix ink or ground, don't delete the epic colour from the words.

What I will not refute: Q2 reserved for Andrew; default `TierColor` stands until he speaks; no-plate as the only D1 shape with no engine attack; `BadgeTextColor` dead; alpha strip before any plate; D2 after a real donor; D3 parked; never hide F.

### (3) Fairness

The synthesis got my hill (Q2) and my no-plate vote right, and the conductor correctly refused to rule Q2. Distortion: folding D4 into "unanimous D1" and listing me under agreement #1 as if I wanted colour stripped off the sentence. I ranked **D3 above D1-as-stated (beige + unit)**; I did **not** rank D3 above no-plate + coloured names. That ranking sentence without the D4 qualifier makes me look like an overlay maximalist.

### (4) Ship 3.1.0

**PlainText one unit `Tier N · G`, no `<mark>`, keep `AffixNameColor=TierColor`; do not ship D1a-left, D1b, or D1c.**

🟣➤⚫ Cursor-Grok

---
Conductor note (not seat text): the two distortions named are accepted and corrected in SYNTHESIS v3 (Grok's vote is no-plate + TierColor; its D3 ranking applies only to "D1 with a neutral sentence"). One factual slip in the seat's own text: the render seat's PNGs draw the plate OVER the glyphs (z-index above the text, confirmed by Gemini and Kimi from the markup), not behind; the seat's point that a CSS approximation cannot prove the engine's behaviour stands regardless.
