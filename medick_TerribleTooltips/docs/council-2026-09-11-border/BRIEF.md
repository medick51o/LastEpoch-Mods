# COUNCIL BRIEF — the BORDER problem (2026-09-11 14:36, blind; facts only, no conductor opinion)

## Andrew's words, verbatim (14:33)
"this looks great, but also want to add to give players opition to stay T7 instead of Tier 7 or T1 instead of Tier 1. also i noticed i really like how in the mock up we had borders around the tier and the grade, noticed it didnt copy over into the game … is there anyway we can make a boarder or make the background a different color, im trying to get a way from the dark background and white text for Tier and grade i like the new setup where the test is the color of the the pallete we are trying to advertise its the border you pitched to me that really sold me on it, but it doesnt look like it transfered over into the game, try your hardest to make it work, call up the council to get that to work"

## The target (approved 08:41, renders\design-2026-09-11-D2-t6t7-rule-1x.png)
Per affix line: a small pill/outline around `Tier N | G` (or `TN | G`), thin BORDER in a muted purple-grey (mockup CSS: `background:#1e1526; border:1px solid #5a4670; border-radius:5px; padding:2px 9px`), ink = tier colour for the tier word and grade colour for the letter, sentence beside it (T1–T5 white, T6/T7 #C990FF). Andrew's ask now: a BORDER (or a different background) around the unit, NOT a dark plate with white ink. The current in-game look (3.1.0-dev, SignalStyle=PlainText) = coloured text `Tier 5 · A` with no border at all.

## Hard facts from the game (MelonLoader Latest.log, 14:22–14:33, Last Epoch 1.4.7, Unity 6000 IL2CPP, TextMeshPro)
- Each affix line is its OWN TextMeshProUGUI: `TooltipItem/Content/body-content/Body/Prefix N/Text` (and `Suffix N/Text`), margin=(32.40, 3.00, 19.20, -4.80). Text is our composed rich text, e.g. `<color=#A807FF>Tier 5</color><color=#8a8478>·</color><color=#FA9E3D>A</color>  58% increased …`.
- Each affix row ALSO carries a sibling Image: `Body/Prefix N/corrupted-border` — sprite `T_UI_Tooltip_Corrupted_Effect`, type Sliced, border=(4,8,73,7), rect 447.6×42, raycast=False, with a child `corrupted-border (1)` (sprite null, Sliced). Present on every Prefix/Suffix row. Activity state not logged (the probe's "donor try 1: parent/sibling Image of first affix TMP → none" suggests it was inactive or filtered).
- Sliced (9-slice) sprites in the tooltip: `item-tooltip-main` border (12,5,12,55) · `item-tooltip-main-fill` (4,4,4,55) · `item-tooltip-foreground-border-high-tier` (12,20,12,67) — a frame-BORDER sprite · `item-tooltip-ruined-gradient`.
- D2 probe result (PillProbe.cs, 3.1.0-dev): dump 566 images / 260 TMPs; donor try 1 (sibling Image of affix TMP) → none; try 2 (sibling of Range TMP) → none; try 3 accepted `Header/Name/lorehighlight` (sprite `dialogueOptionGlow`, Simple); then `FAIL at UpdateLayout: InvalidOperationException: accepted donor clone lost its Image sprite` — the probe latched off for the session, text path unharmed. So: a cloned Image can be created and parented, but the clone's sprite was gone by the time UpdateLayout ran (pooled/reset, or the clone was re-initialised by the game's own component on that donor).
- TMP law (ARCHAEOLOGY.md, in-game verified): `<mark>` draws its quad OVER the glyphs (no padding, square). `<u>`/`<s>` draw lines in the text colour. No border tag exists in TMP rich text. `<sprite>` needs a sprite asset on the TMP; none is known. `<font-weight>`, `<size>`, `<cspace>`, `<pos>`, `<voffset>` work. The mod re-invokes the game's own layout after rewriting text; TMP `textInfo` (per-character vertex positions) is available after `ForceMeshUpdate`.
- Proven elsewhere in the fleet: Terrible Inventory (`medick_Advanced_Inventory\src\NativeClone.cs`) clones native buttons/rows into game panels; Unity `Sprite.Create(Texture2D, rect, pivot, ppu, extrude, meshType, border)` builds a 9-slice sprite from a runtime-drawn texture; `Image.type = Sliced` respects it; `RectTransform.SetSiblingIndex(0)` under the same parent draws BEHIND a later sibling (the TMP), so an Image sibling placed before `Text` renders under the glyphs — the one thing `<mark>` cannot do.
- Constraints: no edits to forbidden hooks (OpenItemTooltip, UpdatePrefixAndSuffixesText); anything per-frame must be cheap (the stutter bug is fresh); pooled tooltips reuse TMPs; comparison tooltips show two at once; controller mode exists; LeHud + Fallen Improved Tooltips coexist; HDR capture washed light `<mark>` quads to white last night.

## Questions (answer each; rank; give the in-game test that proves or kills it)
Q1. What is the most reliable way to draw a thin palette-coloured BORDER (transparent or near-transparent centre) around exactly the `Tier N | G` unit on every affix line, UNDER the glyphs, in this tooltip? Candidates you must cost (add your own): (a) runtime-drawn 9-slice border sprite on a new Image sibling placed before the row's TMP, positioned from TMP textInfo bounds of the unit; (b) re-purpose the row's own `corrupted-border` sibling (clone or recolour) — is its sprite a border?; (c) clone `background-fill-ornamentation` (`item-tooltip-foreground-border-high-tier`) shrunk to the unit; (d) pure rich text: bracket glyphs `⟦ ⟧`/`[ ]`/box-drawing in the tier colour + `<u>` rule; (e) a low-alpha `<mark>` in the tier colour as a different-background fallback (the D1c alpha strip); (f) anything else.
Q2. Why did the probe's clone lose its sprite at UpdateLayout, and what makes a clone survive (copy sprite+material+color explicitly after Instantiate? destroy the donor's own MonoBehaviours? never clone, always build fresh)?
Q3. Positioning: how to get the unit's pixel bounds robustly (textInfo characterInfo for the first K visible chars, bottomLeft/topRight, after ForceMeshUpdate) and keep it right on re-layout, pooled reuse, two tooltips, UI scale 100/125/150%, controller mode. When must it be recomputed, and what is the cheapest trigger (the mod already has a dirty window)?
Q4. Colour rule for the border: tier colour at what alpha, or the mockup's neutral `#5a4670`? One border per line or two (tier and grade separately)? Keep it calm; the sentence rule (white / #C990FF) is settled.
Q5. `T7` vs `Tier 7` as a player option: name the pref (`TierWord = Spelled|Compact`), and does the compact form change the border geometry or wrap risk?
Q6. Failure policy: what must the mod do when the border cannot be placed (degrade to the plain coloured text with no border, log once) and what is the one line in the log that proves the path taken?
Output: SIGNED read, ≤120 lines, verdict DONE / DONE_WITH_CONCERNS, ranked candidates with a one-line in-game test each. Facts over opinions; say NOT PROVEN where it is.
