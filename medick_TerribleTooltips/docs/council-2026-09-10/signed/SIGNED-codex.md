# 🔵 Codex — council read (2026-09-10, blind, read-only sandbox, bug-prover lens)
threadId 01a08e6b-d7db-7c21-bb38-e830771f9f05 · default model (gpt-5.6-sol lineage)

DONE_WITH_CONCERNS

## Manifest (MD5 computed by the seat)
BRIEF c14d88804a1d701ad4181dbd54029465 · DIFF cf9f4e3c21c64b693437be4811e0b37f · log 019f1b203fce5a2670b32030eafa4a198 · cfg 4278f5798a2554fff021b27e876c079ef · csproj 704cb3f23dcc2b59f9142f3401a9e3ea · Il2CppLE.dll 5fef07528053f3a463eda5255ec9a985 · src: AaronsHouse 8ad7f75c… AffixInjector df8b37d6… BuildInfo 61081f45… Colors f618270a… FilterRuleTooltip df527124… GroundLabels 007871fb… NativeSettings d4f9877d… Prefs ebbd6f26… SettingsUi bbac9fd0… TerribleTooltipsApi 93822285… TerribleTooltipsMod 3ac02bf6… TooltipRecolor 6eb761cd… (also read its own ~/.codex dispatch skill files)

## Findings
| Rank | Claim | Failure mechanism | Verify-by / repair direction |
|---|---|---|---|
| MATERIAL | Q2 — The scan gate can permanently miss fresh Alt-written text. The visible `[1B]` is the injector's uncomposed input. | Patch_FormatAffix prepends a rich-text bracket (AffixInjector.cs:83-98); TMP shows it as `[1B]`. Only RunScan→Compose removes it (TooltipRecolor.cs:327-330,391-395,471-478,518-620). The hole: (1) an earlier scan sets s_dirty=false before inspecting the scene (327-335); (2) Alt/native refresh writes fresh bracketed text without calling SetAs*Tooltip; (3) FormatAffix does not MarkDirty; (4) with AlwaysShowRanges=true, Alt need not flip the native range switch, so no dirty signal from there (cfg:27-30, TooltipRecolor.cs:106-107,121-140); (5) ReRenderFromOriginals refuses the markerless TMP and drops its cached original (215-231); (6) ShouldScan incl. the 0.5 s fallback is evaluated only from the UpdateLayout postfix (86-104,311-321). No later UpdateLayout → no scan ever. | Log frame/order of FormatAffix, TMP assignment, UpdateLayout, RunScan, OnLateUpdate while pressing Alt with both pins on. Repair: mark dirty whenever an injector changes __result, or hook a content-complete method; evaluate the fallback from OnLateUpdate while a tooltip is active; do not clear the dirty generation before the content write is observed. |
| MATERIAL | Q4 — The stutter gate reduces the normal case but can relapse to a full-scene scan every frame. | 0.5 s fallback still scans ~2/s during ground hover (70-103,327-335). Every-frame relapse: a cached s_originals TMP stays active but is repurposed/cleared by the game → ShouldScan sees no marker → true (92-100); PruneOriginals keeps it (337-338,504-511); if its new text is empty/non-affix the composer skips it without restoring a marker or deleting the entry (364-395) → next UpdateLayout repeats the scene scan. | Hover item A then item B with fewer/empty affix widgets while moving; count RunScan per frame. Repair: generation-scope caches to the active tooltip/item; remove active markerless entries when the completed scan finds their replacement text is not composable. |
| MATERIAL | Q3 — The new hook covers only one of two FormatAffix overloads; "common ancestor" is unsupported. | Stubs: FormatAffix(ItemDataUnpacked, AffixList.Affix, …) AND FormatAffix(ItemDataUnpacked, ItemAffix, …). The attribute selects only the latter (AffixInjector.cs:83-86). Any hybrid path via the AffixList.Affix overload cannot reach the postfix. Normal view: unbracketed block w/o Tier:/Range: passes the pre-gates unchanged (TooltipRecolor.cs:383-406); under Alt ComposeUnbracketed synthesizes Tier N, no grade (623-700). Matches the observation. | Patch or instrument both overloads and identify which is called per affix. To get tier AND grade, the uncovered overload must be correlated back to the ItemAffix roll, or an upstream stage that still has ItemAffix must be hooked. ComposeUnbracketed can honestly recover only tier. |
| MINOR | Q5 — Continuation tinting can consume unrelated rich-text styling after a bracketed line. | lastTierHex stays set for the rest of the TMP; every later non-sealed/tier/range line has colour tags stripped and is wrapped in the tier colour (535-563,602-617). No structural boundary proves later lines belong to the affix; headers/enchantment/Fallen-styled lines in the same TMP lose native styling (DIFF:538-574). | Test TMPs with continuation text, enchantment-only text, headers, Fallen/LeHud markup. Repair: identify affix-block boundaries or limit tinting to the known stat-line count. |
| NOT PROVEN | Q1 — No normal 3.0.1-diff path prevents an ordinary dropped GroundItemLabel from being created, activated or written. | Postfix starts the unchanged one-frame coroutine (GroundLabels.cs:70-85). FilterOnly + NO FILTER → filter==null → coroutine exits (107-110; Api.cs:18-28) WITHOUT clearing/hiding the label. MarkDirty runs only after item-tooltip setup. The scan ignores plain label names (no Tier:/Range:/bracket) and skips marked ones (TooltipRecolor.cs:360-395). Tier cache writes colours only (204-207). | Highest-value discriminator: the pending 3.0.0 rollback result. Then 3.0.1 in a clean mod profile recording SetGroundTooltipText, itemText.text, activeInHierarchy. |
| NOT PROVEN | Q1 — A conditional cross-surface blanking mechanism exists, but its prerequisite is unsupported. | RunScan scans every active scene TMP, not tooltip descendants (327-360): an UNMARKED single-line label containing the literal "Range:" would be classified a standalone range widget and replaced by the 4-ZWSP marker (433-450); 4 ZWSP contains GroundLabels' 3-ZWSP marker, so the label coroutine's guard exits (GroundLabels.cs:25-27,82-101); LateUpdate keeps enforcing the blank (176-200); the dirty catch-up adds a timing window (165-173). No evidence ordinary labels contain "Range:", so it does not explain every drop. | Observe every GroundItemLabel.itemText before/after each RunScan. Repair regardless: scope RunScan to UITooltipItem descendants, exclude TMPs owned by GroundItemLabel, and use markers that are not substrings of each other. |
| NOT PROVEN | Q1 — Ranked non-diff candidates: (1) native ground-label subsystem / global display state; (2) Fallen's failed reference initialisation; (3) configuration. | Symptom (models exist, every label absent, label-pickup gone) is upstream of TT's post-write augmentation; no TT exception, 9/9 patches. Fallen: two loading-time NREs (log:6-15); no supplied body links it to label creation. Config: obsolete `GroundLabels="FilterOnly"` key is not a registered entry (Prefs.cs:102-107); filter-only exit touches nothing. | Record the rollback result; reproduce with Fallen disabled and TT 3.0.1 unchanged; then a clean profile. |
| NOT PROVEN | Q3 — The exact native route of the two-stat affixes cannot be proved from stubs. | Postfix could receive an empty result (AffixInjector.cs:96), throw inside the swallowed try/catch (94-105), or the block could be assembled elsewhere. | Instrument both overload postfixes with affix identity/result; preserve exceptions instead of swallowing. |
| NOT PROVEN | Q5 — No additional diff-specific break for ground labels, filter-rule number, LeHud/Fallen, or the pins. | Composer excludes the 'requires' TMP (378-381); new patch is on FormatAffix, not the forbidden OpenItemTooltip. | Regression matrix across hover surfaces, Alt, pins, rule modes, Fallen/LeHud on/off. |
| NOT PROVEN | Doctrine citation mismatch: installed codex-spine v1.1 has no §8.9. | — | Supply the intended SPINE revision. |

No files were modified, no game was run, no helper was spawned.

🔵 Codex
