# TICKET-3.1.0-border-fix1 — border exists but is INVISIBLE in-game (15:30, team lead)
In-hand 15:26–15:28 (Andrew, screenshots; log = LOG-border-firstlook.txt): `[UnitBorder] placed 6/6 rows path=runtime-9slice` etc. on every hover, no OFF, no exception — yet NO outline visible on any row (SDR capture, 1440p-class). Tint, PlainText unit, rule number all correct. One `unit wrapped; row skipped` on a long line (expected).
Diagnosis from source (UnitBorder.cs): (1) `new GameObject(...)` gets layer 0 (Default); every other object under the tooltip is on the canvas' layer (UI=5). A Screen-Space-Camera canvas with a UI-only culling mask never draws layer-0 graphics → culled silently. (2) Ring = 1 texel at ppu 100 on the OUTERMOST texel row/column with bilinear+clamp → ≤1 px hairline; colour #5A4670 on a ~#1A1224 plate = very low contrast. (3) Geometry (textInfo → world → parent local, anchor top-left) reads correct; do not touch.

## WRITE SET (fence): `medick_TerribleTooltips\src\UnitBorder.cs`, `medick_TerribleTooltips\src\Prefs.cs` (new keys only), `CHANGELOG.md`. Nothing else. Hooks untouched.
1. LAYER: after SetParent, `border.layer = tmp.gameObject.layer;` (copy the TMP's layer). Log ONCE at first placement: `[UnitBorder] canvas=<name> mode=<renderMode> cam=<camera name or none> mask=<cullingMask or -> layer(tmp)=<n> layer(border)=<n>`.
2. STROKE: texture 32×32, ring 2 texels wide (x<2||x>=30||y<2||y>=30), corners cleared for the 2×2 outer corner only, `border=(6,6,6,6)`, ppu stays 100; `image.pixelsPerUnitMultiplier = 1f`. Add pref `BorderThickness` int default 2 (clamp 1..4): ring width in texels = value (rebuild the sprite when it changes; sprite cached per thickness).
3. COLOUR: neutral default brightened to `#8A74A8` at alpha 0.9 (still muted purple-grey, reads on the dark plate); `TierColor` mode alpha 0.75. Add pref `BorderDebug` bool default false: when true, ring colour = `#FF00FF` alpha 1, `fillCenter = true` with the sprite's centre at 25% alpha, and every placement logs `[UnitBorder] dbg row=<n> pos=(x,y) size=(w,h) sib=<i>/<count> layer=<n> enabled=<b> cull=<canvasRenderer.cull> sprite=<name or null>`. The debug ring answers "not rendered" vs "too faint" in one hover.
4. GEOMETRY GUARD: if size.x < 4 or size.y < 4 → skip the row and log once `[UnitBorder] degenerate rect on row <n> (w×h)`; count it in the placed n/m line as a skip.
5. VISIBILITY: after configuring the Image call `image.enabled = false; image.enabled = true;` once at creation (forces graphic registration in IL2CPP), then `image.SetAllDirty()`.
6. CHANGELOG line. Version stays 3.1.0-dev.
Verify: `dotnet build … -c Release -p:DeployToMods=false`, 0 warnings. Containment vs baseline-310borderfix1.md5 = exactly the write set. STAGE ONLY, then write `docs\build-2026-09-11\BUILD-310border-fix1.md` (DLL path/size/md5, the new log lines, in-hand steps: set `BorderDebug = true` for the first look, then false). Gemini review AFTER the signal file (Andrew is testing live; dev build; 3.0.2 restore kept).
