# BUILD-310dev — REPAIR ROUND 1 staged 2026-09-11 14:24, NOT deployed by the builder lane (deploy = team lead)
**Current staged DLL (post-repair):** `medick_TerribleTooltips\medick_TerribleTooltips\bin\Release\net6.0\medick_Terrible_Tooltips.dll` — 83,456 B — md5 5670b8181f54bcd757b53929679ed71c — v3.1.0-dev. Conductor verify: `dotnet build -c Release -p:DeployToMods=false` → 0 warnings, 0 errors.
**What the game is running right now (deployed by the team lead 14:21):** the pre-repair 14:17 DLL, 82,944 B, md5 a2a47c050707585d28436d17b73916fb. Its known defects (Gemini round 1): a FALSE startup warning "unregistered preference keys in cfg: GreaterAffixTint, PillProbe, PillTiers" (harmless); unbracketed multi-stat T6/T7 affixes tinted on their first line only; an invalid GreaterAffixTint is rewritten to #C990FF on save. All three fixed in the staged DLL above.
Builder: 🔵 Codex thread 01a0924e-619d-7352-be84-37833623f8fe (build + repair round 1). Containment vs baseline-310dev.md5: FAILED on exactly TooltipRecolor.cs, Prefs.cs, FilterRuleTooltip.cs, Colors.cs, CHANGELOG.md, README.md (= the write set); everything else OK, PillProbe.cs and every hook file untouched; hook/scan code in TooltipRecolor.cs byte-identical to baseline (grep-hash match, both rounds). Delta: DELTA-310dev.patch (259 lines, post-repair).
Files: TooltipRecolor.cs (ComposeCleanLine tint rule; continuation path in Compose and ComposeUnbracketed follow the first line's rule; sealed/Alt 90% dimming; `GreaterAffixHex()` validator, one warning per session, no pref write-back) · Prefs.cs (`AffixNameColorMode.GreaterAffix` appended and default; `Style` default PlainText; `GreaterAffixTint` default from Colors; WarnOrphanedKeys knows the three new keys) · FilterRuleTooltip.cs:118 (`<size=120%><b>` gold) · Colors.cs (`GreaterAffixTintDefault = "#C990FF"` + header; dead `BadgeTextColor` removed, zero callers) · CHANGELOG (v3.1.0-dev entry) · README (#C990FF legend line).
Also in this DLL (from the earlier staged probe, unchanged): PillProbe.cs, `PillProbe=false`, `PillTiers=All`.
Example PlainText lines under the new defaults:
- T4/C: `<color=#77ACFF>Tier 4</color><color=#8a8478>·</color><color=#77ACFF>C</color>  sentence`
- T7/A: `<color=#FF44FF>Tier 7</color><color=#8a8478>·</color><color=#FA9E3D>A</color>  <color=#C990FF>sentence</color>`
Skipped: item D partially — composer-owned Range rows and tier annotations are at 90%; the standalone game-owned Tier widget text is untouched (no hook added, per ticket). Nothing else skipped.
Existing cfg users with `AffixNameColor=TierColor` / `SignalStyle=Badge` keep their look; only fresh defaults change. cfg key names and enum orders unchanged.
Review: round 1 = Gemini APPROVE_WITH_NOTES (0 BLOCKER, 2 MATERIAL, 1 MINOR) → all three repaired; round 2 Gemini verification 14:26 = **APPROVE**, no residual findings (REVIEW-310dev-gemini.md). Repair rounds used 1 of 2. This DLL is review-clean and ready for the team lead to swap in (game closed).
