# BUILD-310border — STAGED 2026-09-11 15:10, review-clean, NOT deployed (deploy = team lead; game closed first)
DLL: `medick_TerribleTooltips\medick_TerribleTooltips\bin\Release\net6.0\medick_Terrible_Tooltips.dll` — **94,208 B** — md5 **d9dd90f8778bde89a21f8cb44c2d8e15** — v3.1.0-dev. Conductor verify: 0 warnings, 0 errors. Containment vs baseline-310border.md5: exactly Prefs.cs, TerribleTooltipsMod.cs, TooltipRecolor.cs, CHANGELOG.md + NEW UnitBorder.cs (614 lines); hooks/scan-gate/marker byte-identical; PillProbe.cs untouched. Delta: DELTA-310border.patch (746 lines). Review: 🟢 Gemini APPROVE (0 BLOCKER / 0 MATERIAL / 1 cosmetic MINOR), 0 repair rounds (REVIEW-310border-gemini.md). Council: docs\council-2026-09-11-border\SYNTHESIS.md (6 seats).
What it does: on every affix line in PlainText style, a thin `#5A4670` outline (runtime-drawn 9-slice, transparent centre, ~1 unit stroke, rounded hint) drawn UNDER the coloured `Tier N · G` glyphs as a sibling Image before the row's Text; sized from TMP glyph bounds; nothing cloned from the game. New cfg keys (all appended, existing keys untouched): `UnitBorder = true` · `TierWord = "Spelled"` (`"Compact"` → `T7 · A`) · `BorderColorMode = "Neutral"` (`"TierColor"` → tier colour at 60%). Sentence rule from 3.1.0-dev unchanged (T1–T5 white, T6/T7 #C990FF). Badge style: no border (border is PlainText-only). Ground labels and EHG's deep-view "Tier:" text untouched by TierWord.
Log lines (MelonLoader console):
- PASS: `[UnitBorder] placed n/m rows path=runtime-9slice` (n = rows outlined, m = candidate rows; expect n = m on a normal rare/exalted)
- DEGRADED: `[UnitBorder] OFF (<stage>: <exception>: <message>) — plain coloured text` (feature latched off for the session; the coloured text stays exactly as 3.1.0-dev)
- Once-only diagnostics you may also see: `[UnitBorder] linkInfo empty; using prefix count` (fallback locator engaged, still fine) · `[UnitBorder] unit wrapped; row skipped` (a very long line at high UI scale; that row gets no border)
- Off: `[UnitBorder] off — borders removed` (master or UnitBorder toggled off at runtime)
Andrew's 5 in-hand steps (game CLOSED for step 1):
1. Team lead swaps the DLL above into Mods (3.0.2 restore stays). Cfg needs no edit: `UnitBorder=true` is the dev default and gets written on first quit.
2. Launch; console must say `v3.1.0-dev loaded (10/10 patches)` (the unit border is the 10th patch). 9/10 with a warning naming "unit border" = the hook did not bind → tell me the warning line.
3. Hover an exalted with several affixes. Look for a thin muted-purple outline hugging `Tier N · G` on every affix line, the coloured text on top, the sentence untouched beside it. Console: one `[UnitBorder] placed n/m rows …` line. Screenshot (SDR; and HDR if that is how you capture).
4. Abuse it: hover a second item fast, open the stash and compare, hold Alt, walk with a ground tooltip up. Any leftover or doubled outline, any hitch = tell me which step.
5. Options (game closed between edits): `TierWord = "Compact"` → `T7 · A` in the outline; `BorderColorMode = "TierColor"` → the outline in the tier colour; `UnitBorder = false` → back to plain coloured text.
Open: exact look at 125/150% UI scale, HDR behaviour of a real Image (council: expected fine, NOT PROVEN), the `<link>` locator in this TMP build (fallback exists), PillProbe stays off and superseded.
